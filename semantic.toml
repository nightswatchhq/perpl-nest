# semantic.toml - what this nest's data *means* (RFC-0016). Read by the MCP `schema`
# tool, the admin UI, and the scaffolded skill. Edit descriptions freely; the
# `[table.*.footguns]` are DERIVED from the ABI and regenerated - leave them be.

schema_version = 1

[nest]
description = "The `perpl-nest` nest on monad. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__account_created]
description = "`AccountCreated(address,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per AccountCreated(address,uint256) event"

[table.perpl__account_created.columns]
account = "The `account` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
id = "The `id` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__account_created.footguns]
big_ints = ["id"]
overflows_dec = ["id"]

[table.perpl__account_fee_tier_set]
description = "`AccountFeeTierSet(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per AccountFeeTierSet(uint256,uint256) event"

[table.perpl__account_fee_tier_set.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tier = "The `tier` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__account_fee_tier_set.footguns]
big_ints = [
    "accountId",
    "tier",
]
overflows_dec = [
    "accountId",
    "tier",
]

[table.perpl__account_freeze]
description = "`AccountFreeze(uint256,uint8)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per AccountFreeze(uint256,uint8) event"

[table.perpl__account_freeze.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
status = "The `status` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__account_freeze.footguns]
big_ints = ["accountId"]
overflows_dec = ["accountId"]

[table.perpl__account_frozen]
description = "`AccountFrozen(uint8)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per AccountFrozen(uint8) event"

[table.perpl__account_frozen.columns]
status = "The `status` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__account_liquidation_credit]
description = "`AccountLiquidationCredit(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per AccountLiquidationCredit(uint256,uint256,uint256,uint256) event"

[table.perpl__account_liquidation_credit.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endBalanceCNS = "The `endBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startBalanceCNS = "The `startBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__account_liquidation_credit.footguns]
big_ints = [
    "perpId",
    "accountId",
    "startBalanceCNS",
    "endBalanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "startBalanceCNS",
    "endBalanceCNS",
]

[table.perpl__admin_changed]
description = "`AdminChanged(address,address)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per AdminChanged(address,address) event"

[table.perpl__admin_changed.columns]
newAdmin = "The `newAdmin` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
previousAdmin = "The `previousAdmin` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__administrator_updated]
description = "`AdministratorUpdated(address,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per AdministratorUpdated(address,bool) event"

[table.perpl__administrator_updated.columns]
added = "The `added` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `added = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `added = 'true'` or `CAST(added AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
administrator = "The `administrator` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__administrator_updated.footguns]
bools = ["added"]

[table.perpl__amount_exceeds_available_balance]
description = "`AmountExceedsAvailableBalance(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per AmountExceedsAvailableBalance(uint256,uint256,uint256) event"

[table.perpl__amount_exceeds_available_balance.columns]
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
availableBalanceCNS = "The `availableBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__amount_exceeds_available_balance.footguns]
big_ints = [
    "amountCNS",
    "availableBalanceCNS",
    "balanceCNS",
]
overflows_dec = [
    "amountCNS",
    "availableBalanceCNS",
    "balanceCNS",
]

[table.perpl__bankruptcy_price_prevents_deleverage]
description = "`BankruptcyPricePreventsDeleverage(uint256,uint256,uint8,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BankruptcyPricePreventsDeleverage(uint256,uint256,uint8,uint256,uint256) event"

[table.perpl__bankruptcy_price_prevents_deleverage.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
bankruptcyPricePNS = "The `bankruptcyPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__bankruptcy_price_prevents_deleverage.footguns]
big_ints = [
    "perpId",
    "accountId",
    "bankruptcyPricePNS",
    "markPricePNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "bankruptcyPricePNS",
    "markPricePNS",
]

[table.perpl__beacon_upgraded]
description = "`BeaconUpgraded(address)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BeaconUpgraded(address) event"

[table.perpl__beacon_upgraded.columns]
beacon = "The `beacon` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__block_status_changed]
description = "`BlockStatusChanged(address,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BlockStatusChanged(address,bool) event"

[table.perpl__block_status_changed.columns]
addr = "The `addr` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
blocked = "The `blocked` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `blocked = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `blocked = 'true'` or `CAST(blocked AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__block_status_changed.footguns]
bools = ["blocked"]

[table.perpl__borrow_margin_not_met_after_dec_collateral]
description = "`BorrowMarginNotMetAfterDecCollateral(uint256,uint256,int256,int256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BorrowMarginNotMetAfterDecCollateral(uint256,uint256,int256,int256) event"

[table.perpl__borrow_margin_not_met_after_dec_collateral.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
bmrCNS = "The `bmrCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fmvAfterCNS = "The `fmvAfterCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__borrow_margin_not_met_after_dec_collateral.footguns]
big_ints = [
    "perpId",
    "accountId",
    "bmrCNS",
    "fmvAfterCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "bmrCNS",
    "fmvAfterCNS",
]

[table.perpl__buy_to_liquidate_buyer_restricted]
description = "`BuyToLiquidateBuyerRestricted(uint256,address)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BuyToLiquidateBuyerRestricted(uint256,address) event"

[table.perpl__buy_to_liquidate_buyer_restricted.columns]
buyer = "The `buyer` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__buy_to_liquidate_buyer_restricted.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__buy_to_liquidate_params_updated]
description = "`BuyToLiquidateParamsUpdated(uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BuyToLiquidateParamsUpdated(uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__buy_to_liquidate_params_updated.columns]
buyerAmtPer100K = "The `buyerAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
insAmtPer100K = "The `insAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
protAmtPer100K = "The `protAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
userAmtPer100K = "The `userAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__buy_to_liquidate_params_updated.footguns]
big_ints = [
    "perpId",
    "insAmtPer100K",
    "userAmtPer100K",
    "buyerAmtPer100K",
    "protAmtPer100K",
]
overflows_dec = [
    "perpId",
    "insAmtPer100K",
    "userAmtPer100K",
    "buyerAmtPer100K",
    "protAmtPer100K",
]

[table.perpl__buy_to_liquidate_restriction_updated]
description = "`BuyToLiquidateRestrictionUpdated(uint256,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BuyToLiquidateRestrictionUpdated(uint256,bool) event"

[table.perpl__buy_to_liquidate_restriction_updated.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
restrictBuyers = "The `restrictBuyers` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `restrictBuyers = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `restrictBuyers = 'true'` or `CAST(restrictBuyers AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__buy_to_liquidate_restriction_updated.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]
bools = ["restrictBuyers"]

[table.perpl__buy_to_liquidate_settled]
description = "`BuyToLiquidateSettled(uint256,uint256,uint8,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BuyToLiquidateSettled(uint256,uint256,uint8,uint256,uint256,int256,uint256) event"

[table.perpl__buy_to_liquidate_settled.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderType = "The `orderType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
realizedPricePNS = "The `realizedPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__buy_to_liquidate_settled.footguns]
big_ints = [
    "perpId",
    "accountId",
    "realizedPricePNS",
    "lotLNS",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "realizedPricePNS",
    "lotLNS",
    "amountCNS",
    "balanceCNS",
]

[table.perpl__buy_to_liquidate_slippage_exceeded]
description = "`BuyToLiquidateSlippageExceeded(uint256,uint256,uint8,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BuyToLiquidateSlippageExceeded(uint256,uint256,uint8,uint256,uint256) event"

[table.perpl__buy_to_liquidate_slippage_exceeded.columns]
limitPricePNS = "The `limitPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posAccountId = "The `posAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__buy_to_liquidate_slippage_exceeded.footguns]
big_ints = [
    "perpId",
    "posAccountId",
    "markPricePNS",
    "limitPricePNS",
]
overflows_dec = [
    "perpId",
    "posAccountId",
    "markPricePNS",
    "limitPricePNS",
]

[table.perpl__buy_to_liquidate_started]
description = "`BuyToLiquidateStarted(uint256,uint256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BuyToLiquidateStarted(uint256,uint256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__buy_to_liquidate_started.columns]
leverageHdths = "The `leverageHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
limitPricePNS = "The `limitPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liquidatorId = "The `liquidatorId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxNegPnlCollatBPS = "The `maxNegPnlCollatBPS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posAccountId = "The `posAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
requestedLotLNS = "The `requestedLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__buy_to_liquidate_started.footguns]
big_ints = [
    "perpId",
    "posAccountId",
    "liquidatorId",
    "requestedLotLNS",
    "leverageHdths",
    "limitPricePNS",
    "maxNegPnlCollatBPS",
]
overflows_dec = [
    "perpId",
    "posAccountId",
    "liquidatorId",
    "requestedLotLNS",
    "leverageHdths",
    "limitPricePNS",
    "maxNegPnlCollatBPS",
]

[table.perpl__buy_to_liquidate_threshold_updated]
description = "`BuyToLiquidateThresholdUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per BuyToLiquidateThresholdUpdated(uint256,uint256) event"

[table.perpl__buy_to_liquidate_threshold_updated.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
thresholdPer100K = "The `thresholdPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__buy_to_liquidate_threshold_updated.footguns]
big_ints = [
    "perpId",
    "thresholdPer100K",
]
overflows_dec = [
    "perpId",
    "thresholdPer100K",
]

[table.perpl__cancel_existing_invalid_close_orders]
description = "`CancelExistingInvalidCloseOrders(uint256,uint8,uint8)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CancelExistingInvalidCloseOrders(uint256,uint8,uint8) event"

[table.perpl__cancel_existing_invalid_close_orders.columns]
lockedLotLNS = "The `lockedLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedPositionType = "The `lockedPositionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
newPositionType = "The `newPositionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__cancel_existing_invalid_close_orders.footguns]
big_ints = ["lockedLotLNS"]
overflows_dec = ["lockedLotLNS"]

[table.perpl__cannot_adjust_entry_price_to_dec_collateral]
description = "`CannotAdjustEntryPriceToDecCollateral(uint256,uint256,uint256,uint256,uint256,int256,uint8)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CannotAdjustEntryPriceToDecCollateral(uint256,uint256,uint256,uint256,uint256,int256,uint8) event"

[table.perpl__cannot_adjust_entry_price_to_dec_collateral.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
adjustedEntryPricePNS = "The `adjustedEntryPricePNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
adjustmentAmountCNS = "The `adjustmentAmountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
entryPricePNS = "The `entryPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__cannot_adjust_entry_price_to_dec_collateral.footguns]
big_ints = [
    "perpId",
    "accountId",
    "amountCNS",
    "adjustmentAmountCNS",
    "entryPricePNS",
    "adjustedEntryPricePNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "amountCNS",
    "adjustmentAmountCNS",
    "entryPricePNS",
    "adjustedEntryPricePNS",
]

[table.perpl__cant_buy_to_liquidate]
description = "`CantBuyToLiquidate(uint256,uint256,uint8,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CantBuyToLiquidate(uint256,uint256,uint8,uint256,uint256,uint256,uint256) event"

[table.perpl__cant_buy_to_liquidate.columns]
bkptPricePNS = "The `bkptPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
bsLiqPricePNS = "The `bsLiqPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liqPricePNS = "The `liqPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posAccountId = "The `posAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__cant_buy_to_liquidate.footguns]
big_ints = [
    "perpId",
    "posAccountId",
    "markPricePNS",
    "bsLiqPricePNS",
    "liqPricePNS",
    "bkptPricePNS",
]
overflows_dec = [
    "perpId",
    "posAccountId",
    "markPricePNS",
    "bsLiqPricePNS",
    "liqPricePNS",
    "bkptPricePNS",
]

[table.perpl__cant_change_close_order]
description = "`CantChangeCloseOrder(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CantChangeCloseOrder(uint256,uint256,uint256) event"

[table.perpl__cant_change_close_order.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__cant_change_close_order.footguns]
big_ints = [
    "perpId",
    "orderId",
    "accountId",
]
overflows_dec = [
    "perpId",
    "orderId",
    "accountId",
]

[table.perpl__cant_deleverage_against_opposing_positions]
description = "`CantDeleverageAgainstOpposingPositions(uint256,uint256,bool,uint8,uint256,uint256[])` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CantDeleverageAgainstOpposingPositions(uint256,uint256,bool,uint8,uint256,uint256[]) event"

[table.perpl__cant_deleverage_against_opposing_positions.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
deleveragePricePNS = "The `deleveragePricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
forceClose = "The `forceClose` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `forceClose = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `forceClose = 'true'` or `CAST(forceClose AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
sortedPositionIds = "The `sortedPositionIds` uint256[] parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__cant_deleverage_against_opposing_positions.footguns]
big_ints = [
    "perpId",
    "accountId",
    "deleveragePricePNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "deleveragePricePNS",
]
bools = ["forceClose"]

[table.perpl__cant_liquidate_pos_above_m_m_r]
description = "`CantLiquidatePosAboveMMR(uint256,uint256,uint8,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CantLiquidatePosAboveMMR(uint256,uint256,uint8,uint256,uint256) event"

[table.perpl__cant_liquidate_pos_above_m_m_r.columns]
liqPricePNS = "The `liqPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posAccountId = "The `posAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__cant_liquidate_pos_above_m_m_r.footguns]
big_ints = [
    "perpId",
    "posAccountId",
    "markPricePNS",
    "liqPricePNS",
]
overflows_dec = [
    "perpId",
    "posAccountId",
    "markPricePNS",
    "liqPricePNS",
]

[table.perpl__change_expired_order_needs_new_expiry]
description = "`ChangeExpiredOrderNeedsNewExpiry(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ChangeExpiredOrderNeedsNewExpiry(uint256,uint256,uint256,uint256) event"

[table.perpl__change_expired_order_needs_new_expiry.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
expiryBlock = "The `expiryBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__change_expired_order_needs_new_expiry.footguns]
big_ints = [
    "perpId",
    "orderId",
    "accountId",
    "expiryBlock",
]
overflows_dec = [
    "perpId",
    "orderId",
    "accountId",
    "expiryBlock",
]

[table.perpl__clearing_expired_order]
description = "`ClearingExpiredOrder(uint256,uint256,uint256,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ClearingExpiredOrder(uint256,uint256,uint256,uint256,uint256,int256,uint256) event"

[table.perpl__clearing_expired_order.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAccountId = "The `recyclerAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAmountCNS = "The `recyclerAmountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerBalanceCNS = "The `recyclerBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__clearing_expired_order.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]

[table.perpl__clearing_frozen_account_order]
description = "`ClearingFrozenAccountOrder(uint256,uint256,uint256,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ClearingFrozenAccountOrder(uint256,uint256,uint256,uint256,uint256,int256,uint256) event"

[table.perpl__clearing_frozen_account_order.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAccountId = "The `recyclerAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAmountCNS = "The `recyclerAmountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerBalanceCNS = "The `recyclerBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__clearing_frozen_account_order.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]

[table.perpl__clearing_invalid_close_order]
description = "`ClearingInvalidCloseOrder(uint256,uint256,uint256,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ClearingInvalidCloseOrder(uint256,uint256,uint256,uint256,uint256,int256,uint256) event"

[table.perpl__clearing_invalid_close_order.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAccountId = "The `recyclerAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAmountCNS = "The `recyclerAmountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerBalanceCNS = "The `recyclerBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__clearing_invalid_close_order.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]

[table.perpl__clearing_remaining_order_lock_beyond_balance]
description = "`ClearingRemainingOrderLockBeyondBalance(uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ClearingRemainingOrderLockBeyondBalance(uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,int256,uint256) event"

[table.perpl__clearing_remaining_order_lock_beyond_balance.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
excessiveLockedBalCNS = "The `excessiveLockedBalCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAccountId = "The `recyclerAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAmountCNS = "The `recyclerAmountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerBalanceCNS = "The `recyclerBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
remainingLotLNS = "The `remainingLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__clearing_remaining_order_lock_beyond_balance.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderId",
    "pricePNS",
    "remainingLotLNS",
    "lockedBalanceCNS",
    "excessiveLockedBalCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderId",
    "pricePNS",
    "remainingLotLNS",
    "lockedBalanceCNS",
    "excessiveLockedBalCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]

[table.perpl__clearing_self_matching_order]
description = "`ClearingSelfMatchingOrder(uint256,uint256,uint256,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ClearingSelfMatchingOrder(uint256,uint256,uint256,uint256,uint256,int256,uint256) event"

[table.perpl__clearing_self_matching_order.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAccountId = "The `recyclerAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAmountCNS = "The `recyclerAmountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerBalanceCNS = "The `recyclerBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__clearing_self_matching_order.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]

[table.perpl__close_order_exceeds_position]
description = "`CloseOrderExceedsPosition(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CloseOrderExceedsPosition(uint256,uint256) event"

[table.perpl__close_order_exceeds_position.columns]
orderLotLNS = "The `orderLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posLotLNS = "The `posLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__close_order_exceeds_position.footguns]
big_ints = [
    "posLotLNS",
    "orderLotLNS",
]
overflows_dec = [
    "posLotLNS",
    "orderLotLNS",
]

[table.perpl__close_order_position_mismatch]
description = "`CloseOrderPositionMismatch(uint8,uint8)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CloseOrderPositionMismatch(uint8,uint8) event"

[table.perpl__close_order_position_mismatch.columns]
orderType = "The `orderType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__collateral_decrease_declined]
description = "`CollateralDecreaseDeclined(uint256,uint256,string)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CollateralDecreaseDeclined(uint256,uint256,string) event"

[table.perpl__collateral_decrease_declined.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
reason = "The `reason` string parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__collateral_decrease_declined.footguns]
big_ints = [
    "perpId",
    "accountId",
]
overflows_dec = [
    "perpId",
    "accountId",
]

[table.perpl__collateral_decrease_request_cancelled]
description = "`CollateralDecreaseRequestCancelled(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CollateralDecreaseRequestCancelled(uint256,uint256) event"

[table.perpl__collateral_decrease_request_cancelled.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__collateral_decrease_request_cancelled.footguns]
big_ints = [
    "perpId",
    "accountId",
]
overflows_dec = [
    "perpId",
    "accountId",
]

[table.perpl__collateral_decrease_request_expired]
description = "`CollateralDecreaseRequestExpired(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CollateralDecreaseRequestExpired(uint256,uint256,uint256,uint256) event"

[table.perpl__collateral_decrease_request_expired.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
blockTS = "The `blockTS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
expiryTS = "The `expiryTS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__collateral_decrease_request_expired.footguns]
big_ints = [
    "perpId",
    "accountId",
    "expiryTS",
    "blockTS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "expiryTS",
    "blockTS",
]

[table.perpl__collateral_decrease_requested]
description = "`CollateralDecreaseRequested(uint256,uint256,uint256,uint256,bool,uint8,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CollateralDecreaseRequested(uint256,uint256,uint256,uint256,bool,uint8,uint256,uint256) event"

[table.perpl__collateral_decrease_requested.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
clampToMaximum = "The `clampToMaximum` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `clampToMaximum = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `clampToMaximum = 'true'` or `CAST(clampToMaximum AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
entryPricePNS = "The `entryPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
expiryTS = "The `expiryTS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__collateral_decrease_requested.footguns]
big_ints = [
    "perpId",
    "accountId",
    "expiryTS",
    "amountCNS",
    "entryPricePNS",
    "lotLNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "expiryTS",
    "amountCNS",
    "entryPricePNS",
    "lotLNS",
]
bools = ["clampToMaximum"]

[table.perpl__collateral_deposit]
description = "`CollateralDeposit(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CollateralDeposit(uint256,uint256,uint256) event"

[table.perpl__collateral_deposit.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__collateral_deposit.footguns]
big_ints = [
    "accountId",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "accountId",
    "amountCNS",
    "balanceCNS",
]

[table.perpl__collateral_withdrawal]
description = "`CollateralWithdrawal(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CollateralWithdrawal(uint256,uint256,uint256) event"

[table.perpl__collateral_withdrawal.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__collateral_withdrawal.footguns]
big_ints = [
    "accountId",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "accountId",
    "amountCNS",
    "balanceCNS",
]

[table.perpl__contract_added]
description = "`ContractAdded(uint256,string,string,uint8,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,bool,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ContractAdded(uint256,string,string,uint8,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,bool,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__contract_added.columns]
absFundingClampPctPer100K = "The `absFundingClampPctPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
basePricePNS = "The `basePricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
btlBuyerAmtPer100K = "The `btlBuyerAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
btlInsAmtPer100K = "The `btlInsAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
btlPriceThreshPer100K = "The `btlPriceThreshPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
btlRestrictBuyers = "The `btlRestrictBuyers` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `btlRestrictBuyers = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `btlRestrictBuyers = 'true'` or `CAST(btlRestrictBuyers AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
btlUserAmtPer100K = "The `btlUserAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
dcpBorrowThreshHdths = "The `dcpBorrowThreshHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
initMarginFracHdths = "The `initMarginFracHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
insAmtPer100K = "The `insAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liqInsAmtPer100K = "The `liqInsAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liqUserAmtPer100K = "The `liqUserAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotDecimals = "The `lotDecimals` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maintMarginFracHdths = "The `maintMarginFracHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
makerFeePer100K = "The `makerFeePer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
marginTol = "The `marginTol` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
marginTolDecimals = "The `marginTolDecimals` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxOpenInterestLNS = "The `maxOpenInterestLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
name = "The `name` string parameter. (seeded from the ABI - edit semantic.toml to improve this)"
numPerpetuals = "The `numPerpetuals` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
overColDescentThreshHdths = "The `overColDescentThreshHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
permCancelMinOrders = "The `permCancelMinOrders` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
permCancelSegment = "The `permCancelSegment` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
priceDecimals = "The `priceDecimals` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
priceTolPer100K = "The `priceTolPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
refPriceMaxAgeSec = "The `refPriceMaxAgeSec` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
status = "The `status` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
symbol = "The `symbol` string parameter. (seeded from the ABI - edit semantic.toml to improve this)"
takerFeePer100K = "The `takerFeePer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
unityDescentThreshHdths = "The `unityDescentThreshHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__contract_added.footguns]
big_ints = [
    "perpId",
    "basePricePNS",
    "priceDecimals",
    "lotDecimals",
    "takerFeePer100K",
    "makerFeePer100K",
    "initMarginFracHdths",
    "maintMarginFracHdths",
    "maxOpenInterestLNS",
    "unityDescentThreshHdths",
    "overColDescentThreshHdths",
    "dcpBorrowThreshHdths",
    "priceTolPer100K",
    "marginTol",
    "marginTolDecimals",
    "refPriceMaxAgeSec",
    "absFundingClampPctPer100K",
    "permCancelMinOrders",
    "permCancelSegment",
    "insAmtPer100K",
    "liqInsAmtPer100K",
    "liqUserAmtPer100K",
    "btlPriceThreshPer100K",
    "btlInsAmtPer100K",
    "btlUserAmtPer100K",
    "btlBuyerAmtPer100K",
    "numPerpetuals",
]
overflows_dec = [
    "perpId",
    "basePricePNS",
    "priceDecimals",
    "lotDecimals",
    "takerFeePer100K",
    "makerFeePer100K",
    "initMarginFracHdths",
    "maintMarginFracHdths",
    "maxOpenInterestLNS",
    "unityDescentThreshHdths",
    "overColDescentThreshHdths",
    "dcpBorrowThreshHdths",
    "priceTolPer100K",
    "marginTol",
    "marginTolDecimals",
    "refPriceMaxAgeSec",
    "absFundingClampPctPer100K",
    "permCancelMinOrders",
    "permCancelSegment",
    "insAmtPer100K",
    "liqInsAmtPer100K",
    "liqUserAmtPer100K",
    "btlPriceThreshPer100K",
    "btlInsAmtPer100K",
    "btlUserAmtPer100K",
    "btlBuyerAmtPer100K",
    "numPerpetuals",
]
bools = ["btlRestrictBuyers"]

[table.perpl__contract_added_v2]
description = "`ContractAddedV2(uint256,string,string,uint8,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,bool,uint256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ContractAddedV2(uint256,string,string,uint8,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,bool,uint256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__contract_added_v2.columns]
absFundingClampPctPer100K = "The `absFundingClampPctPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
basePricePNS = "The `basePricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
btlBuyerAmtPer100K = "The `btlBuyerAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
btlInsAmtPer100K = "The `btlInsAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
btlPriceThreshPer100K = "The `btlPriceThreshPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
btlRestrictBuyers = "The `btlRestrictBuyers` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `btlRestrictBuyers = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `btlRestrictBuyers = 'true'` or `CAST(btlRestrictBuyers AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
btlUserAmtPer100K = "The `btlUserAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
dcpBorrowThreshHdths = "The `dcpBorrowThreshHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
initMarginFracHdths = "The `initMarginFracHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
insAmtPer100K = "The `insAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liqInsAmtPer100K = "The `liqInsAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liqUserAmtPer100K = "The `liqUserAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotDecimals = "The `lotDecimals` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maintMarginFracHdths = "The `maintMarginFracHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
marginTol = "The `marginTol` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
marginTolDecimals = "The `marginTolDecimals` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxOpenInterestLNS = "The `maxOpenInterestLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
name = "The `name` string parameter. (seeded from the ABI - edit semantic.toml to improve this)"
numPerpetuals = "The `numPerpetuals` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
overColDescentThreshHdths = "The `overColDescentThreshHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
permCancelMinOrders = "The `permCancelMinOrders` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
permCancelSegment = "The `permCancelSegment` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpFeeSchedId = "The `perpFeeSchedId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
priceDecimals = "The `priceDecimals` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
priceTolPer100K = "The `priceTolPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
refPriceMaxAgeSec = "The `refPriceMaxAgeSec` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
status = "The `status` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
symbol = "The `symbol` string parameter. (seeded from the ABI - edit semantic.toml to improve this)"
unityDescentThreshHdths = "The `unityDescentThreshHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__contract_added_v2.footguns]
big_ints = [
    "perpId",
    "basePricePNS",
    "priceDecimals",
    "lotDecimals",
    "initMarginFracHdths",
    "maintMarginFracHdths",
    "maxOpenInterestLNS",
    "unityDescentThreshHdths",
    "overColDescentThreshHdths",
    "dcpBorrowThreshHdths",
    "priceTolPer100K",
    "marginTol",
    "marginTolDecimals",
    "refPriceMaxAgeSec",
    "absFundingClampPctPer100K",
    "permCancelMinOrders",
    "permCancelSegment",
    "insAmtPer100K",
    "liqInsAmtPer100K",
    "liqUserAmtPer100K",
    "btlPriceThreshPer100K",
    "btlInsAmtPer100K",
    "btlUserAmtPer100K",
    "btlBuyerAmtPer100K",
    "numPerpetuals",
    "perpFeeSchedId",
]
overflows_dec = [
    "perpId",
    "basePricePNS",
    "priceDecimals",
    "lotDecimals",
    "initMarginFracHdths",
    "maintMarginFracHdths",
    "maxOpenInterestLNS",
    "unityDescentThreshHdths",
    "overColDescentThreshHdths",
    "dcpBorrowThreshHdths",
    "priceTolPer100K",
    "marginTol",
    "marginTolDecimals",
    "refPriceMaxAgeSec",
    "absFundingClampPctPer100K",
    "permCancelMinOrders",
    "permCancelSegment",
    "insAmtPer100K",
    "liqInsAmtPer100K",
    "liqUserAmtPer100K",
    "btlPriceThreshPer100K",
    "btlInsAmtPer100K",
    "btlUserAmtPer100K",
    "btlBuyerAmtPer100K",
    "numPerpetuals",
    "perpFeeSchedId",
]
bools = ["btlRestrictBuyers"]

[table.perpl__contract_link_feed_updated]
description = "`ContractLinkFeedUpdated(uint256,bytes32)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ContractLinkFeedUpdated(uint256,bytes32) event"

[table.perpl__contract_link_feed_updated.columns]
feedId = "The `feedId` bytes32 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__contract_link_feed_updated.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__contract_not_operational]
description = "`ContractNotOperational(uint256,uint8)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ContractNotOperational(uint256,uint8) event"

[table.perpl__contract_not_operational.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
status = "The `status` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__contract_not_operational.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__contract_paused]
description = "`ContractPaused(uint256,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ContractPaused(uint256,bool) event"

[table.perpl__contract_paused.columns]
paused = "The `paused` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `paused = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `paused = 'true'` or `CAST(paused AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__contract_paused.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]
bools = ["paused"]

[table.perpl__contract_removed]
description = "`ContractRemoved(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ContractRemoved(uint256) event"

[table.perpl__contract_removed.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__contract_removed.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__contract_version_set]
description = "`ContractVersionSet(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ContractVersionSet(uint256,uint256,uint256) event"

[table.perpl__contract_version_set.columns]
major = "The `major` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
minor = "The `minor` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
patch = "The `patch` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__contract_version_set.footguns]
big_ints = [
    "major",
    "minor",
    "patch",
]
overflows_dec = [
    "major",
    "minor",
    "patch",
]

[table.perpl__crosses_book]
description = "`CrossesBook(uint256,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per CrossesBook(uint256,bool) event"

[table.perpl__crosses_book.columns]
maxOrdersChecked = "The `maxOrdersChecked` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `maxOrdersChecked = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `maxOrdersChecked = 'true'` or `CAST(maxOrdersChecked AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
minAskOrMaxBidPNS = "The `minAskOrMaxBidPNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__crosses_book.footguns]
big_ints = ["minAskOrMaxBidPNS"]
overflows_dec = ["minAskOrMaxBidPNS"]
bools = ["maxOrdersChecked"]

[table.perpl__dcp_borrow_thresh_updated]
description = "`DcpBorrowThreshUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per DcpBorrowThreshUpdated(uint256,uint256) event"

[table.perpl__dcp_borrow_thresh_updated.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
threshHdths = "The `threshHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__dcp_borrow_thresh_updated.footguns]
big_ints = [
    "perpId",
    "threshHdths",
]
overflows_dec = [
    "perpId",
    "threshHdths",
]

[table.perpl__decrease_collateral_beyond_mark_price]
description = "`DecreaseCollateralBeyondMarkPrice(uint256,uint256,uint8,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per DecreaseCollateralBeyondMarkPrice(uint256,uint256,uint8,uint256,uint256) event"

[table.perpl__decrease_collateral_beyond_mark_price.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
impactAdjPricePNS = "The `impactAdjPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__decrease_collateral_beyond_mark_price.footguns]
big_ints = [
    "perpId",
    "accountId",
    "impactAdjPricePNS",
    "markPricePNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "impactAdjPricePNS",
    "markPricePNS",
]

[table.perpl__default_perp_fee_schedule_set]
description = "`DefaultPerpFeeScheduleSet(uint256,uint256[8],uint256[8])` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per DefaultPerpFeeScheduleSet(uint256,uint256[8],uint256[8]) event"

[table.perpl__default_perp_fee_schedule_set.columns]
feeSchedId = "The `feeSchedId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
makerFeesPer100K = "The `makerFeesPer100K` uint256[8] parameter. (seeded from the ABI - edit semantic.toml to improve this)"
takerFeesPer100K = "The `takerFeesPer100K` uint256[8] parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__default_perp_fee_schedule_set.footguns]
big_ints = ["feeSchedId"]
overflows_dec = ["feeSchedId"]

[table.perpl__default_rwa_fee_schedule_set]
description = "`DefaultRwaFeeScheduleSet(uint256,uint256[8],uint256[8])` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per DefaultRwaFeeScheduleSet(uint256,uint256[8],uint256[8]) event"

[table.perpl__default_rwa_fee_schedule_set.columns]
feeSchedId = "The `feeSchedId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
makerFeesPer100K = "The `makerFeesPer100K` uint256[8] parameter. (seeded from the ABI - edit semantic.toml to improve this)"
takerFeesPer100K = "The `takerFeesPer100K` uint256[8] parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__default_rwa_fee_schedule_set.footguns]
big_ints = ["feeSchedId"]
overflows_dec = ["feeSchedId"]

[table.perpl__deleverage_position_list_empty]
description = "`DeleveragePositionListEmpty(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per DeleveragePositionListEmpty(uint256,uint256) event"

[table.perpl__deleverage_position_list_empty.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__deleverage_position_list_empty.footguns]
big_ints = [
    "perpId",
    "accountId",
]
overflows_dec = [
    "perpId",
    "accountId",
]

[table.perpl__exceeds_last_execution_block]
description = "`ExceedsLastExecutionBlock(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ExceedsLastExecutionBlock(uint256) event"

[table.perpl__exceeds_last_execution_block.columns]
lastExecutionBlock = "The `lastExecutionBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__exceeds_last_execution_block.footguns]
big_ints = ["lastExecutionBlock"]
overflows_dec = ["lastExecutionBlock"]

[table.perpl__exchange_halted]
description = "`ExchangeHalted(bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ExchangeHalted(bool) event"

[table.perpl__exchange_halted.columns]
halted = "The `halted` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `halted = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `halted = 'true'` or `CAST(halted AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__exchange_halted.footguns]
bools = ["halted"]

[table.perpl__exchange_initialized]
description = "`ExchangeInitialized(address,address,uint256,uint256,uint256,uint256,uint256,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ExchangeInitialized(address,address,uint256,uint256,uint256,uint256,uint256,bool) event"

[table.perpl__exchange_initialized.columns]
collateralDecimals = "The `collateralDecimals` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
collateralToken = "The `collateralToken` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
minAccountOpenCNS = "The `minAccountOpenCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recycleFeeCNS = "The `recycleFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
sender = "The `sender` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
whitelistingEnabled = "The `whitelistingEnabled` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `whitelistingEnabled = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `whitelistingEnabled = 'true'` or `CAST(whitelistingEnabled AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
wrlsMinWithdrawLimitCNS = "The `wrlsMinWithdrawLimitCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
wrlsThousandthsTvl = "The `wrlsThousandthsTvl` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__exchange_initialized.footguns]
big_ints = [
    "collateralDecimals",
    "minAccountOpenCNS",
    "wrlsThousandthsTvl",
    "wrlsMinWithdrawLimitCNS",
    "recycleFeeCNS",
]
overflows_dec = [
    "collateralDecimals",
    "minAccountOpenCNS",
    "wrlsThousandthsTvl",
    "wrlsMinWithdrawLimitCNS",
    "recycleFeeCNS",
]
bools = ["whitelistingEnabled"]

[table.perpl__fee_params_updated]
description = "`FeeParamsUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per FeeParamsUpdated(uint256,uint256) event"

[table.perpl__fee_params_updated.columns]
insAmtPer100K = "The `insAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__fee_params_updated.footguns]
big_ints = [
    "perpId",
    "insAmtPer100K",
]
overflows_dec = [
    "perpId",
    "insAmtPer100K",
]

[table.perpl__fee_schedule_set]
description = "`FeeScheduleSet(uint256,uint256[8],uint256[8])` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per FeeScheduleSet(uint256,uint256[8],uint256[8]) event"

[table.perpl__fee_schedule_set.columns]
feeSchedId = "The `feeSchedId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
makerFeesPer100K = "The `makerFeesPer100K` uint256[8] parameter. (seeded from the ABI - edit semantic.toml to improve this)"
takerFeesPer100K = "The `takerFeesPer100K` uint256[8] parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__fee_schedule_set.footguns]
big_ints = ["feeSchedId"]
overflows_dec = ["feeSchedId"]

[table.perpl__funding_clamp_pct_updated]
description = "`FundingClampPctUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per FundingClampPctUpdated(uint256,uint256) event"

[table.perpl__funding_clamp_pct_updated.columns]
clampPctPer100k = "The `clampPctPer100k` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__funding_clamp_pct_updated.footguns]
big_ints = [
    "perpId",
    "clampPctPer100k",
]
overflows_dec = [
    "perpId",
    "clampPctPer100k",
]

[table.perpl__funding_event_completed]
description = "`FundingEventCompleted(uint256,uint256,int256,int256,uint256,int48,int48,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per FundingEventCompleted(uint256,uint256,int256,int256,uint256,int48,int48,bool) event"

[table.perpl__funding_event_completed.columns]
actualRatePct100k = "The `actualRatePct100k` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
allowOverwrite = "The `allowOverwrite` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `allowOverwrite = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `allowOverwrite = 'true'` or `CAST(allowOverwrite AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
fundingEventBlock = "The `fundingEventBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fundingPaymentPNS = "The `fundingPaymentPNS` int48 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fundingPricePNS = "The `fundingPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fundingSumPNS = "The `fundingSumPNS` int48 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
specifiedRatePct100k = "The `specifiedRatePct100k` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__funding_event_completed.footguns]
big_ints = [
    "perpId",
    "fundingEventBlock",
    "specifiedRatePct100k",
    "actualRatePct100k",
    "fundingPricePNS",
]
overflows_dec = [
    "perpId",
    "fundingEventBlock",
    "specifiedRatePct100k",
    "actualRatePct100k",
    "fundingPricePNS",
]
bools = ["allowOverwrite"]

[table.perpl__funding_event_set_too_early]
description = "`FundingEventSetTooEarly(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per FundingEventSetTooEarly(uint256,uint256,uint256) event"

[table.perpl__funding_event_set_too_early.columns]
blockNumber = "The `blockNumber` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fundingEventBlock = "The `fundingEventBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__funding_event_set_too_early.footguns]
big_ints = [
    "perpId",
    "blockNumber",
    "fundingEventBlock",
]
overflows_dec = [
    "perpId",
    "blockNumber",
    "fundingEventBlock",
]

[table.perpl__funding_price_exceeds_tol]
description = "`FundingPriceExceedsTol(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per FundingPriceExceedsTol(uint256,uint256,uint256,uint256) event"

[table.perpl__funding_price_exceeds_tol.columns]
fundingPricePNS = "The `fundingPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
oraclePNS = "The `oraclePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tolerancePer100k = "The `tolerancePer100k` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__funding_price_exceeds_tol.footguns]
big_ints = [
    "perpId",
    "fundingPricePNS",
    "oraclePNS",
    "tolerancePer100k",
]
overflows_dec = [
    "perpId",
    "fundingPricePNS",
    "oraclePNS",
    "tolerancePer100k",
]

[table.perpl__funding_sum_already_set]
description = "`FundingSumAlreadySet(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per FundingSumAlreadySet(uint256,uint256,uint256,uint256) event"

[table.perpl__funding_sum_already_set.columns]
fundingEventBlock = "The `fundingEventBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fundingSumOffset = "The `fundingSumOffset` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
storageIndex = "The `storageIndex` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__funding_sum_already_set.footguns]
big_ints = [
    "perpId",
    "fundingEventBlock",
    "storageIndex",
    "fundingSumOffset",
]
overflows_dec = [
    "perpId",
    "fundingEventBlock",
    "storageIndex",
    "fundingSumOffset",
]

[table.perpl__funding_sum_scaling_exp_updated]
description = "`FundingSumScalingExpUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per FundingSumScalingExpUpdated(uint256,uint256) event"

[table.perpl__funding_sum_scaling_exp_updated.columns]
newExp = "The `newExp` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__funding_sum_scaling_exp_updated.footguns]
big_ints = [
    "perpId",
    "newExp",
]
overflows_dec = [
    "perpId",
    "newExp",
]

[table.perpl__ignore_oracle_updated]
description = "`IgnoreOracleUpdated(uint256,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per IgnoreOracleUpdated(uint256,bool) event"

[table.perpl__ignore_oracle_updated.columns]
ignOracle = "The `ignOracle` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `ignOracle = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `ignOracle = 'true'` or `CAST(ignOracle AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__ignore_oracle_updated.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]
bools = ["ignOracle"]

[table.perpl__immediate_or_cancel_executed]
description = "`ImmediateOrCancelExecuted(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ImmediateOrCancelExecuted(uint256,uint256) event"

[table.perpl__immediate_or_cancel_executed.columns]
totalLotLNS = "The `totalLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
unmatchedLotLNS = "The `unmatchedLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__immediate_or_cancel_executed.footguns]
big_ints = [
    "unmatchedLotLNS",
    "totalLotLNS",
]
overflows_dec = [
    "unmatchedLotLNS",
    "totalLotLNS",
]

[table.perpl__increase_position_collateral]
description = "`IncreasePositionCollateral(uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per IncreasePositionCollateral(uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__increase_position_collateral.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionDepositCNS = "The `positionDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__increase_position_collateral.footguns]
big_ints = [
    "perpId",
    "accountId",
    "positionDepositCNS",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "positionDepositCNS",
    "amountCNS",
    "balanceCNS",
]

[table.perpl__initial_margin_fraction_updated]
description = "`InitialMarginFractionUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InitialMarginFractionUpdated(uint256,uint256) event"

[table.perpl__initial_margin_fraction_updated.columns]
initMarginFracHdths = "The `initMarginFracHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__initial_margin_fraction_updated.footguns]
big_ints = [
    "perpId",
    "initMarginFracHdths",
]
overflows_dec = [
    "perpId",
    "initMarginFracHdths",
]

[table.perpl__initialized]
description = "`Initialized(uint8)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per Initialized(uint8) event"

[table.perpl__initialized.columns]
version = "The `version` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__insolvent_position_cannot_be_forced_close]
description = "`InsolventPositionCannotBeForcedClose(uint256,uint256,uint8,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InsolventPositionCannotBeForcedClose(uint256,uint256,uint8,uint256,uint256) event"

[table.perpl__insolvent_position_cannot_be_forced_close.columns]
bankruptcyPricePNS = "The `bankruptcyPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posAccountId = "The `posAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__insolvent_position_cannot_be_forced_close.footguns]
big_ints = [
    "perpId",
    "posAccountId",
    "bankruptcyPricePNS",
    "markPricePNS",
]
overflows_dec = [
    "perpId",
    "posAccountId",
    "bankruptcyPricePNS",
    "markPricePNS",
]

[table.perpl__insufficient_funds_to_dec_collateral]
description = "`InsufficientFundsToDecCollateral(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InsufficientFundsToDecCollateral(uint256,uint256,uint256,uint256) event"

[table.perpl__insufficient_funds_to_dec_collateral.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
withdrawMaxCNS = "The `withdrawMaxCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__insufficient_funds_to_dec_collateral.footguns]
big_ints = [
    "perpId",
    "accountId",
    "amountCNS",
    "withdrawMaxCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "amountCNS",
    "withdrawMaxCNS",
]

[table.perpl__insuficient_funds_for_recycle_fee]
description = "`InsuficientFundsForRecycleFee(uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InsuficientFundsForRecycleFee(uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__insuficient_funds_for_recycle_fee.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedCNS = "The `lockedCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recycleFeeCNS = "The `recycleFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__insuficient_funds_for_recycle_fee.footguns]
big_ints = [
    "perpId",
    "accountId",
    "balanceCNS",
    "lockedCNS",
    "recycleFeeCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "balanceCNS",
    "lockedCNS",
    "recycleFeeCNS",
]

[table.perpl__insurance_payment_for_settlement]
description = "`InsurancePaymentForSettlement(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InsurancePaymentForSettlement(uint256,uint256,uint256) event"

[table.perpl__insurance_payment_for_settlement.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
insPaymentCNS = "The `insPaymentCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__insurance_payment_for_settlement.footguns]
big_ints = [
    "perpId",
    "accountId",
    "insPaymentCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "insPaymentCNS",
]

[table.perpl__invalid_account_frozen_order]
description = "`InvalidAccountFrozenOrder(uint8,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InvalidAccountFrozenOrder(uint8,bool) event"

[table.perpl__invalid_account_frozen_order.columns]
immediateOrCancel = "The `immediateOrCancel` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `immediateOrCancel = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `immediateOrCancel = 'true'` or `CAST(immediateOrCancel AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
orderType = "The `orderType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__invalid_account_frozen_order.footguns]
bools = ["immediateOrCancel"]

[table.perpl__invalid_bankruptcy_price]
description = "`InvalidBankruptcyPrice(uint256,uint256,uint256,uint256,uint256,int256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InvalidBankruptcyPrice(uint256,uint256,uint256,uint256,uint256,int256) event"

[table.perpl__invalid_bankruptcy_price.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
depositCNS = "The `depositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liqLotLNS = "The `liqLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posPricePNS = "The `posPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
premiumPnlCNS = "The `premiumPnlCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__invalid_bankruptcy_price.footguns]
big_ints = [
    "perpId",
    "accountId",
    "depositCNS",
    "posPricePNS",
    "liqLotLNS",
    "premiumPnlCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "depositCNS",
    "posPricePNS",
    "liqLotLNS",
    "premiumPnlCNS",
]

[table.perpl__invalid_expiry_block]
description = "`InvalidExpiryBlock(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InvalidExpiryBlock(uint256,uint256) event"

[table.perpl__invalid_expiry_block.columns]
blockNumber = "The `blockNumber` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
expiryBlock = "The `expiryBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__invalid_expiry_block.footguns]
big_ints = [
    "expiryBlock",
    "blockNumber",
]
overflows_dec = [
    "expiryBlock",
    "blockNumber",
]

[table.perpl__invalid_link_report_for_contract]
description = "`InvalidLinkReportForContract(uint256,bytes32,bytes32)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InvalidLinkReportForContract(uint256,bytes32,bytes32) event"

[table.perpl__invalid_link_report_for_contract.columns]
perpFeedId = "The `perpFeedId` bytes32 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
reportFeedId = "The `reportFeedId` bytes32 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__invalid_link_report_for_contract.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__invalid_link_report_version]
description = "`InvalidLinkReportVersion(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InvalidLinkReportVersion(uint256,uint256) event"

[table.perpl__invalid_link_report_version.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
reportVersion = "The `reportVersion` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__invalid_link_report_version.footguns]
big_ints = [
    "perpId",
    "reportVersion",
]
overflows_dec = [
    "perpId",
    "reportVersion",
]

[table.perpl__invalid_liquidation_price]
description = "`InvalidLiquidationPrice(uint256,uint256,uint256,uint256,uint256,int256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InvalidLiquidationPrice(uint256,uint256,uint256,uint256,uint256,int256) event"

[table.perpl__invalid_liquidation_price.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
depositCNS = "The `depositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liqLotLNS = "The `liqLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posPricePNS = "The `posPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
premiumPnlCNS = "The `premiumPnlCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__invalid_liquidation_price.footguns]
big_ints = [
    "perpId",
    "accountId",
    "depositCNS",
    "posPricePNS",
    "liqLotLNS",
    "premiumPnlCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "depositCNS",
    "posPricePNS",
    "liqLotLNS",
    "premiumPnlCNS",
]

[table.perpl__invalid_order_id]
description = "`InvalidOrderId(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per InvalidOrderId(uint256,uint256,uint256) event"

[table.perpl__invalid_order_id.columns]
max = "The `max` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
min = "The `min` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__invalid_order_id.footguns]
big_ints = [
    "orderId",
    "min",
    "max",
]
overflows_dec = [
    "orderId",
    "min",
    "max",
]

[table.perpl__last_forwarded_desc_id_reset]
description = "`LastForwardedDescIdReset(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LastForwardedDescIdReset(uint256,uint256) event"

[table.perpl__last_forwarded_desc_id_reset.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
newDescId = "The `newDescId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__last_forwarded_desc_id_reset.footguns]
big_ints = [
    "accountId",
    "newDescId",
]
overflows_dec = [
    "accountId",
    "newDescId",
]

[table.perpl__last_triggered_desc_id_reset]
description = "`LastTriggeredDescIdReset(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LastTriggeredDescIdReset(uint256,uint256) event"

[table.perpl__last_triggered_desc_id_reset.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
newDescId = "The `newDescId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__last_triggered_desc_id_reset.footguns]
big_ints = [
    "accountId",
    "newDescId",
]
overflows_dec = [
    "accountId",
    "newDescId",
]

[table.perpl__link_datastream_configured]
description = "`LinkDatastreamConfigured(address)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LinkDatastreamConfigured(address) event"

[table.perpl__link_datastream_configured.columns]
verifierProxy = "The `verifierProxy` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__link_ds_error_7d7f]
description = "`LinkDsError(uint256,bytes)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LinkDsError(uint256,bytes) event"

[table.perpl__link_ds_error_7d7f.columns]
lowLevelData = "The `lowLevelData` bytes parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__link_ds_error_7d7f.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__link_ds_error_d836]
description = "`LinkDsError(uint256,string)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LinkDsError(uint256,string) event"

[table.perpl__link_ds_error_d836.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
reason = "The `reason` string parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__link_ds_error_d836.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__link_ds_panic]
description = "`LinkDsPanic(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LinkDsPanic(uint256,uint256) event"

[table.perpl__link_ds_panic.columns]
errorCode = "The `errorCode` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__link_ds_panic.footguns]
big_ints = [
    "perpId",
    "errorCode",
]
overflows_dec = [
    "perpId",
    "errorCode",
]

[table.perpl__link_price_updated]
description = "`LinkPriceUpdated(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LinkPriceUpdated(uint256,uint256,uint256) event"

[table.perpl__link_price_updated.columns]
oraclePricePNS = "The `oraclePricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
timestamp = "The `timestamp` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__link_price_updated.footguns]
big_ints = [
    "perpId",
    "oraclePricePNS",
    "timestamp",
]
overflows_dec = [
    "perpId",
    "oraclePricePNS",
    "timestamp",
]

[table.perpl__liquidation_buyer_updated]
description = "`LiquidationBuyerUpdated(address,uint256,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LiquidationBuyerUpdated(address,uint256,bool) event"

[table.perpl__liquidation_buyer_updated.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
added = "The `added` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `added = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `added = 'true'` or `CAST(added AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
liquidationBuyer = "The `liquidationBuyer` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__liquidation_buyer_updated.footguns]
big_ints = ["accountId"]
overflows_dec = ["accountId"]
bools = ["added"]

[table.perpl__liquidation_params_updated]
description = "`LiquidationParamsUpdated(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LiquidationParamsUpdated(uint256,uint256,uint256,uint256) event"

[table.perpl__liquidation_params_updated.columns]
insAmtPer100K = "The `insAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liqAmtPer100K = "The `liqAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
userAmtPer100K = "The `userAmtPer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__liquidation_params_updated.footguns]
big_ints = [
    "perpId",
    "insAmtPer100K",
    "liqAmtPer100K",
    "userAmtPer100K",
]
overflows_dec = [
    "perpId",
    "insAmtPer100K",
    "liqAmtPer100K",
    "userAmtPer100K",
]

[table.perpl__lot_out_of_range]
description = "`LotOutOfRange(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per LotOutOfRange(uint256,uint256) event"

[table.perpl__lot_out_of_range.columns]
maxLotLNS = "The `maxLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
minLotLNS = "The `minLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__lot_out_of_range.footguns]
big_ints = [
    "minLotLNS",
    "maxLotLNS",
]
overflows_dec = [
    "minLotLNS",
    "maxLotLNS",
]

[table.perpl__maintenance_margin_fraction_updated]
description = "`MaintenanceMarginFractionUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MaintenanceMarginFractionUpdated(uint256,uint256) event"

[table.perpl__maintenance_margin_fraction_updated.columns]
maintMarginFracHdths = "The `maintMarginFracHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__maintenance_margin_fraction_updated.footguns]
big_ints = [
    "perpId",
    "maintMarginFracHdths",
]
overflows_dec = [
    "perpId",
    "maintMarginFracHdths",
]

[table.perpl__maker_fee_updated]
description = "`MakerFeeUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MakerFeeUpdated(uint256,uint256) event"

[table.perpl__maker_fee_updated.columns]
makerFeePer100K = "The `makerFeePer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__maker_fee_updated.footguns]
big_ints = [
    "perpId",
    "makerFeePer100K",
]
overflows_dec = [
    "perpId",
    "makerFeePer100K",
]

[table.perpl__maker_order_filled]
description = "`MakerOrderFilled(uint256,uint256,uint256,uint256,uint256,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MakerOrderFilled(uint256,uint256,uint256,uint256,uint256,uint256,uint256,int256,uint256) event"

[table.perpl__maker_order_filled.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
feeCNS = "The `feeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__maker_order_filled.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderId",
    "pricePNS",
    "lotLNS",
    "feeCNS",
    "lockedBalanceCNS",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderId",
    "pricePNS",
    "lotLNS",
    "feeCNS",
    "lockedBalanceCNS",
    "amountCNS",
    "balanceCNS",
]

[table.perpl__maker_order_filled_v2]
description = "`MakerOrderFilledV2(uint256,uint256,uint256,uint256,uint256,uint256,uint256,int256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MakerOrderFilledV2(uint256,uint256,uint256,uint256,uint256,uint256,uint256,int256,uint256,uint256,uint256) event"

[table.perpl__maker_order_filled_v2.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
builderFeeCNS = "The `builderFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
builderId = "The `builderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
feeCNS = "The `feeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__maker_order_filled_v2.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderId",
    "pricePNS",
    "lotLNS",
    "feeCNS",
    "lockedBalanceCNS",
    "amountCNS",
    "balanceCNS",
    "builderId",
    "builderFeeCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderId",
    "pricePNS",
    "lotLNS",
    "feeCNS",
    "lockedBalanceCNS",
    "amountCNS",
    "balanceCNS",
    "builderId",
    "builderFeeCNS",
]

[table.perpl__maker_order_settlement_failed]
description = "`MakerOrderSettlementFailed(uint256,uint256,uint256,uint8,uint256,uint256,uint256,uint256,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MakerOrderSettlementFailed(uint256,uint256,uint256,uint8,uint256,uint256,uint256,uint256,uint256,uint256,int256,uint256) event"

[table.perpl__maker_order_settlement_failed.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxNegPnlCollatBPS = "The `maxNegPnlCollatBPS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderType = "The `orderType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
reason = "The `reason` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAccountId = "The `recyclerAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerAmountCNS = "The `recyclerAmountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recyclerBalanceCNS = "The `recyclerBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__maker_order_settlement_failed.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderId",
    "pricePNS",
    "lotLNS",
    "maxNegPnlCollatBPS",
    "reason",
    "lockedBalanceCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderId",
    "pricePNS",
    "lotLNS",
    "maxNegPnlCollatBPS",
    "reason",
    "lockedBalanceCNS",
    "recyclerAccountId",
    "recyclerAmountCNS",
    "recyclerBalanceCNS",
]

[table.perpl__margin_tol_updated]
description = "`MarginTolUpdated(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MarginTolUpdated(uint256,uint256,uint256) event"

[table.perpl__margin_tol_updated.columns]
decimals = "The `decimals` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tolerance = "The `tolerance` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__margin_tol_updated.footguns]
big_ints = [
    "perpId",
    "tolerance",
    "decimals",
]
overflows_dec = [
    "perpId",
    "tolerance",
    "decimals",
]

[table.perpl__mark_exceeds_tol]
description = "`MarkExceedsTol(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MarkExceedsTol(uint256,uint256,uint256,uint256) event"

[table.perpl__mark_exceeds_tol.columns]
markPNS = "The `markPNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
spotOraclePricePNS = "The `spotOraclePricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tolerancePer100k = "The `tolerancePer100k` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__mark_exceeds_tol.footguns]
big_ints = [
    "perpId",
    "markPNS",
    "spotOraclePricePNS",
    "tolerancePer100k",
]
overflows_dec = [
    "perpId",
    "markPNS",
    "spotOraclePricePNS",
    "tolerancePer100k",
]

[table.perpl__mark_price_age_exceeds_max]
description = "`MarkPriceAgeExceedsMax(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MarkPriceAgeExceedsMax(uint256,uint256,uint256,uint256) event"

[table.perpl__mark_price_age_exceeds_max.columns]
markTimestamp = "The `markTimestamp` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxAgeSec = "The `maxAgeSec` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
timestamp = "The `timestamp` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__mark_price_age_exceeds_max.footguns]
big_ints = [
    "perpId",
    "markTimestamp",
    "timestamp",
    "maxAgeSec",
]
overflows_dec = [
    "perpId",
    "markTimestamp",
    "timestamp",
    "maxAgeSec",
]

[table.perpl__mark_updated]
description = "`MarkUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MarkUpdated(uint256,uint256) event"

[table.perpl__mark_updated.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__mark_updated.footguns]
big_ints = [
    "perpId",
    "pricePNS",
]
overflows_dec = [
    "perpId",
    "pricePNS",
]

[table.perpl__max_matches_reached]
description = "`MaxMatchesReached()` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MaxMatchesReached() event"

[table.perpl__max_open_interest_updated]
description = "`MaxOpenInterestUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MaxOpenInterestUpdated(uint256,uint256) event"

[table.perpl__max_open_interest_updated.columns]
maxOpenInterestLNS = "The `maxOpenInterestLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__max_open_interest_updated.footguns]
big_ints = [
    "perpId",
    "maxOpenInterestLNS",
]
overflows_dec = [
    "perpId",
    "maxOpenInterestLNS",
]

[table.perpl__maximum_account_orders]
description = "`MaximumAccountOrders(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MaximumAccountOrders(uint256,uint256) event"

[table.perpl__maximum_account_orders.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__maximum_account_orders.footguns]
big_ints = [
    "perpId",
    "accountId",
]
overflows_dec = [
    "perpId",
    "accountId",
]

[table.perpl__min_account_open_amount_updated]
description = "`MinAccountOpenAmountUpdated(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MinAccountOpenAmountUpdated(uint256) event"

[table.perpl__min_account_open_amount_updated.columns]
minAccountOpenCNS = "The `minAccountOpenCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__min_account_open_amount_updated.footguns]
big_ints = ["minAccountOpenCNS"]
overflows_dec = ["minAccountOpenCNS"]

[table.perpl__min_post_updated]
description = "`MinPostUpdated(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MinPostUpdated(uint256) event"

[table.perpl__min_post_updated.columns]
minPostCNS = "The `minPostCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__min_post_updated.footguns]
big_ints = ["minPostCNS"]
overflows_dec = ["minPostCNS"]

[table.perpl__min_settle_updated]
description = "`MinSettleUpdated(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MinSettleUpdated(uint256) event"

[table.perpl__min_settle_updated.columns]
minSettleCNS = "The `minSettleCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__min_settle_updated.footguns]
big_ints = ["minSettleCNS"]
overflows_dec = ["minSettleCNS"]

[table.perpl__monitor_administrator_updated]
description = "`MonitorAdministratorUpdated(address,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MonitorAdministratorUpdated(address,bool) event"

[table.perpl__monitor_administrator_updated.columns]
added = "The `added` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `added = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `added = 'true'` or `CAST(added AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
monitorAdministrator = "The `monitorAdministrator` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__monitor_administrator_updated.footguns]
bools = ["added"]

[table.perpl__monitor_pause_attempted]
description = "`MonitorPauseAttempted(uint256,uint8,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per MonitorPauseAttempted(uint256,uint8,bool) event"

[table.perpl__monitor_pause_attempted.columns]
actualStatus = "The `actualStatus` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
transitioned = "The `transitioned` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `transitioned = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `transitioned = 'true'` or `CAST(transitioned AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__monitor_pause_attempted.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]
bools = ["transitioned"]

[table.perpl__oracle_age_exceeds_max]
description = "`OracleAgeExceedsMax(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OracleAgeExceedsMax(uint256,uint256,uint256,uint256) event"

[table.perpl__oracle_age_exceeds_max.columns]
maxAgeSec = "The `maxAgeSec` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
oracleTimestamp = "The `oracleTimestamp` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
timestamp = "The `timestamp` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__oracle_age_exceeds_max.footguns]
big_ints = [
    "perpId",
    "oracleTimestamp",
    "timestamp",
    "maxAgeSec",
]
overflows_dec = [
    "perpId",
    "oracleTimestamp",
    "timestamp",
    "maxAgeSec",
]

[table.perpl__oracle_disabled]
description = "`OracleDisabled(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OracleDisabled(uint256) event"

[table.perpl__oracle_disabled.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__oracle_disabled.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__order_batch_completed]
description = "`OrderBatchCompleted(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderBatchCompleted(uint256) event"

[table.perpl__order_batch_completed.columns]
gasLeft = "The `gasLeft` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_batch_completed.footguns]
big_ints = ["gasLeft"]
overflows_dec = ["gasLeft"]

[table.perpl__order_cancelled]
description = "`OrderCancelled(uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderCancelled(uint256,int256,uint256) event"

[table.perpl__order_cancelled.columns]
amountCNS = "The `amountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_cancelled.footguns]
big_ints = [
    "lockedBalanceCNS",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "lockedBalanceCNS",
    "amountCNS",
    "balanceCNS",
]

[table.perpl__order_cancelled_by_admin]
description = "`OrderCancelledByAdmin(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderCancelledByAdmin(uint256,uint256,uint256,uint256) event"

[table.perpl__order_cancelled_by_admin.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_cancelled_by_admin.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
]

[table.perpl__order_cancelled_by_liquidator]
description = "`OrderCancelledByLiquidator(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderCancelledByLiquidator(uint256,uint256,uint256,uint256) event"

[table.perpl__order_cancelled_by_liquidator.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_cancelled_by_liquidator.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderId",
    "lockedBalanceCNS",
]

[table.perpl__order_changed]
description = "`OrderChanged(uint256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderChanged(uint256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__order_changed.columns]
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
expiryBlock = "The `expiryBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_changed.footguns]
big_ints = [
    "orderId",
    "pricePNS",
    "lotLNS",
    "expiryBlock",
    "lockedBalanceCNS",
    "balanceCNS",
]
overflows_dec = [
    "orderId",
    "pricePNS",
    "lotLNS",
    "expiryBlock",
    "lockedBalanceCNS",
    "balanceCNS",
]

[table.perpl__order_desc_id_too_low]
description = "`OrderDescIdTooLow(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderDescIdTooLow(uint256) event"

[table.perpl__order_desc_id_too_low.columns]
lastOrderDescId = "The `lastOrderDescId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_desc_id_too_low.footguns]
big_ints = ["lastOrderDescId"]
overflows_dec = ["lastOrderDescId"]

[table.perpl__order_does_not_exist]
description = "`OrderDoesNotExist(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderDoesNotExist(uint256,uint256) event"

[table.perpl__order_does_not_exist.columns]
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_does_not_exist.footguns]
big_ints = [
    "perpId",
    "orderId",
]
overflows_dec = [
    "perpId",
    "orderId",
]

[table.perpl__order_extension_rejected]
description = "`OrderExtensionRejected(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderExtensionRejected(uint256,uint256) event"

[table.perpl__order_extension_rejected.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_extension_rejected.footguns]
big_ints = [
    "perpId",
    "accountId",
]
overflows_dec = [
    "perpId",
    "accountId",
]

[table.perpl__order_forwarding_not_allowed]
description = "`OrderForwardingNotAllowed()` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderForwardingNotAllowed() event"

[table.perpl__order_forwarding_updated]
description = "`OrderForwardingUpdated(uint256,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderForwardingUpdated(uint256,bool) event"

[table.perpl__order_forwarding_updated.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
allowed = "The `allowed` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `allowed = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `allowed = 'true'` or `CAST(allowed AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_forwarding_updated.footguns]
big_ints = ["accountId"]
overflows_dec = ["accountId"]
bools = ["allowed"]

[table.perpl__order_placed]
description = "`OrderPlaced(uint256,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderPlaced(uint256,uint256,uint256,int256,uint256) event"

[table.perpl__order_placed.columns]
amountCNS = "The `amountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lockedBalanceCNS = "The `lockedBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_placed.footguns]
big_ints = [
    "orderId",
    "lotLNS",
    "lockedBalanceCNS",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "orderId",
    "lotLNS",
    "lockedBalanceCNS",
    "amountCNS",
    "balanceCNS",
]

[table.perpl__order_post_failed]
description = "`OrderPostFailed(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderPostFailed(uint256) event"

[table.perpl__order_post_failed.columns]
reason = "The `reason` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_post_failed.footguns]
big_ints = ["reason"]
overflows_dec = ["reason"]

[table.perpl__order_request]
description = "`OrderRequest(uint256,uint256,uint256,uint256,uint8,uint256,uint256,uint256,bool,bool,bool,uint256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderRequest(uint256,uint256,uint256,uint256,uint8,uint256,uint256,uint256,bool,bool,bool,uint256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__order_request.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
expiryBlock = "The `expiryBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fillOrKill = "The `fillOrKill` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `fillOrKill = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `fillOrKill = 'true'` or `CAST(fillOrKill AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
gasLeft = "The `gasLeft` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
immediateOrCancel = "The `immediateOrCancel` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `immediateOrCancel = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `immediateOrCancel = 'true'` or `CAST(immediateOrCancel AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
lastExecutionBlock = "The `lastExecutionBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
leverageHdths = "The `leverageHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxMatches = "The `maxMatches` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxNegPnlCollatBPS = "The `maxNegPnlCollatBPS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderDescId = "The `orderDescId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderType = "The `orderType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
postOnly = "The `postOnly` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `postOnly = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `postOnly = 'true'` or `CAST(postOnly AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_request.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderDescId",
    "orderId",
    "pricePNS",
    "lotLNS",
    "expiryBlock",
    "maxMatches",
    "leverageHdths",
    "lastExecutionBlock",
    "amountCNS",
    "maxNegPnlCollatBPS",
    "gasLeft",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderDescId",
    "orderId",
    "pricePNS",
    "lotLNS",
    "expiryBlock",
    "maxMatches",
    "leverageHdths",
    "lastExecutionBlock",
    "amountCNS",
    "maxNegPnlCollatBPS",
    "gasLeft",
]
bools = [
    "postOnly",
    "fillOrKill",
    "immediateOrCancel",
]

[table.perpl__order_request_v2]
description = "`OrderRequestV2(uint256,uint256,uint256,uint256,uint8,uint256,uint256,uint256,bool,bool,bool,uint256,uint256,uint256,uint256,uint256,uint256,bytes)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderRequestV2(uint256,uint256,uint256,uint256,uint8,uint256,uint256,uint256,bool,bool,bool,uint256,uint256,uint256,uint256,uint256,uint256,bytes) event"

[table.perpl__order_request_v2.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
expiryBlock = "The `expiryBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
extension = "The `extension` bytes parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fillOrKill = "The `fillOrKill` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `fillOrKill = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `fillOrKill = 'true'` or `CAST(fillOrKill AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
gasLeft = "The `gasLeft` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
immediateOrCancel = "The `immediateOrCancel` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `immediateOrCancel = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `immediateOrCancel = 'true'` or `CAST(immediateOrCancel AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
lastExecutionBlock = "The `lastExecutionBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
leverageHdths = "The `leverageHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxMatches = "The `maxMatches` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxNegPnlCollatBPS = "The `maxNegPnlCollatBPS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderDescId = "The `orderDescId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderType = "The `orderType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
postOnly = "The `postOnly` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `postOnly = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `postOnly = 'true'` or `CAST(postOnly AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_request_v2.footguns]
big_ints = [
    "perpId",
    "accountId",
    "orderDescId",
    "orderId",
    "pricePNS",
    "lotLNS",
    "expiryBlock",
    "maxMatches",
    "leverageHdths",
    "lastExecutionBlock",
    "amountCNS",
    "maxNegPnlCollatBPS",
    "gasLeft",
]
overflows_dec = [
    "perpId",
    "accountId",
    "orderDescId",
    "orderId",
    "pricePNS",
    "lotLNS",
    "expiryBlock",
    "maxMatches",
    "leverageHdths",
    "lastExecutionBlock",
    "amountCNS",
    "maxNegPnlCollatBPS",
    "gasLeft",
]
bools = [
    "postOnly",
    "fillOrKill",
    "immediateOrCancel",
]

[table.perpl__order_settlement_implies_insolvent]
description = "`OrderSettlementImpliesInsolvent(uint256,uint256,uint8,uint256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderSettlementImpliesInsolvent(uint256,uint256,uint8,uint256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__order_settlement_implies_insolvent.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
addedPosCollatReqCNS = "The `addedPosCollatReqCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderType = "The `orderType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpInsuranceBalCNS = "The `perpInsuranceBalCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpPositionBalCNS = "The `perpPositionBalCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
requestedAmountCNS = "The `requestedAmountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_settlement_implies_insolvent.footguns]
big_ints = [
    "perpId",
    "accountId",
    "pricePNS",
    "lotLNS",
    "perpPositionBalCNS",
    "perpInsuranceBalCNS",
    "addedPosCollatReqCNS",
    "requestedAmountCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "pricePNS",
    "lotLNS",
    "perpPositionBalCNS",
    "perpInsuranceBalCNS",
    "addedPosCollatReqCNS",
    "requestedAmountCNS",
]

[table.perpl__order_size_exceeds_available_size]
description = "`OrderSizeExceedsAvailableSize(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OrderSizeExceedsAvailableSize(uint256,uint256,uint256) event"

[table.perpl__order_size_exceeds_available_size.columns]
availableLotLNS = "The `availableLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderLotLNS = "The `orderLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionLotLNS = "The `positionLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__order_size_exceeds_available_size.footguns]
big_ints = [
    "orderLotLNS",
    "availableLotLNS",
    "positionLotLNS",
]
overflows_dec = [
    "orderLotLNS",
    "availableLotLNS",
    "positionLotLNS",
]

[table.perpl__over_collat_descent_thresh_updated]
description = "`OverCollatDescentThreshUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OverCollatDescentThreshUpdated(uint256,uint256) event"

[table.perpl__over_collat_descent_thresh_updated.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
threshHdths = "The `threshHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__over_collat_descent_thresh_updated.footguns]
big_ints = [
    "perpId",
    "threshHdths",
]
overflows_dec = [
    "perpId",
    "threshHdths",
]

[table.perpl__ownership_transfer_started]
description = "`OwnershipTransferStarted(address,address)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OwnershipTransferStarted(address,address) event"

[table.perpl__ownership_transfer_started.columns]
newOwner = "The `newOwner` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
previousOwner = "The `previousOwner` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__ownership_transferred]
description = "`OwnershipTransferred(address,address)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per OwnershipTransferred(address,address) event"

[table.perpl__ownership_transferred.columns]
newOwner = "The `newOwner` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
previousOwner = "The `previousOwner` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__permissoned_cancel_params_updated]
description = "`PermissonedCancelParamsUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PermissonedCancelParamsUpdated(uint256,uint256) event"

[table.perpl__permissoned_cancel_params_updated.columns]
cancelMinOrders = "The `cancelMinOrders` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
cancelSegment = "The `cancelSegment` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__permissoned_cancel_params_updated.footguns]
big_ints = [
    "cancelMinOrders",
    "cancelSegment",
]
overflows_dec = [
    "cancelMinOrders",
    "cancelSegment",
]

[table.perpl__perp_fee_sched_id_set]
description = "`PerpFeeSchedIdSet(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PerpFeeSchedIdSet(uint256,uint256) event"

[table.perpl__perp_fee_sched_id_set.columns]
feeSchedId = "The `feeSchedId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__perp_fee_sched_id_set.footguns]
big_ints = [
    "perpId",
    "feeSchedId",
]
overflows_dec = [
    "perpId",
    "feeSchedId",
]

[table.perpl__perp_position_bal_credit_positive_severe]
description = "`PerpPositionBalCreditPositiveSevere(uint256,uint256,uint256,uint256,bool,bool,int256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PerpPositionBalCreditPositiveSevere(uint256,uint256,uint256,uint256,bool,bool,int256) event"

[table.perpl__perp_position_bal_credit_positive_severe.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
buyToLiquidate = "The `buyToLiquidate` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `buyToLiquidate = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `buyToLiquidate = 'true'` or `CAST(buyToLiquidate AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
creditPerpBalCNS = "The `creditPerpBalCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
realizedPricePNS = "The `realizedPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
userProceedsToPosition = "The `userProceedsToPosition` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `userProceedsToPosition = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `userProceedsToPosition = 'true'` or `CAST(userProceedsToPosition AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__perp_position_bal_credit_positive_severe.footguns]
big_ints = [
    "perpId",
    "accountId",
    "realizedPricePNS",
    "lotLNS",
    "creditPerpBalCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "realizedPricePNS",
    "lotLNS",
    "creditPerpBalCNS",
]
bools = [
    "userProceedsToPosition",
    "buyToLiquidate",
]

[table.perpl__position_administrator_updated]
description = "`PositionAdministratorUpdated(address,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionAdministratorUpdated(address,bool) event"

[table.perpl__position_administrator_updated.columns]
added = "The `added` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `added = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `added = 'true'` or `CAST(added AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
positionAdministrator = "The `positionAdministrator` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_administrator_updated.footguns]
bools = ["added"]

[table.perpl__position_closed]
description = "`PositionClosed(uint256,uint256,uint8,uint256,int256,int256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionClosed(uint256,uint256,uint8,uint256,int256,int256) event"

[table.perpl__position_closed.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
deltaPnlCNS = "The `deltaPnlCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fundingCNS = "The `fundingCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_closed.footguns]
big_ints = [
    "perpId",
    "accountId",
    "pricePNS",
    "deltaPnlCNS",
    "fundingCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "pricePNS",
    "deltaPnlCNS",
    "fundingCNS",
]

[table.perpl__position_collateral_decreased]
description = "`PositionCollateralDecreased(uint256,uint256,uint8,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionCollateralDecreased(uint256,uint256,uint8,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__position_collateral_decreased.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
decreaseCNS = "The `decreaseCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
effBmfHdths = "The `effBmfHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endDepositCNS = "The `endDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endEntryPricePNS = "The `endEntryPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
impactAdjPricePNS = "The `impactAdjPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startDepositCNS = "The `startDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startEntryPricePNS = "The `startEntryPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_collateral_decreased.footguns]
big_ints = [
    "perpId",
    "accountId",
    "markPricePNS",
    "impactAdjPricePNS",
    "startDepositCNS",
    "endDepositCNS",
    "startEntryPricePNS",
    "endEntryPricePNS",
    "effBmfHdths",
    "decreaseCNS",
    "balanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "markPricePNS",
    "impactAdjPricePNS",
    "startDepositCNS",
    "endDepositCNS",
    "startEntryPricePNS",
    "endEntryPricePNS",
    "effBmfHdths",
    "decreaseCNS",
    "balanceCNS",
]

[table.perpl__position_decreased]
description = "`PositionDecreased(uint256,uint256,uint8,uint256,uint256,uint256,uint256,int256,int256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionDecreased(uint256,uint256,uint8,uint256,uint256,uint256,uint256,int256,int256) event"

[table.perpl__position_decreased.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
deltaPnlCNS = "The `deltaPnlCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endDepositCNS = "The `endDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endLotLNS = "The `endLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fundingCNS = "The `fundingCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startDepositCNS = "The `startDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startLotLNS = "The `startLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_decreased.footguns]
big_ints = [
    "perpId",
    "accountId",
    "startDepositCNS",
    "endDepositCNS",
    "startLotLNS",
    "endLotLNS",
    "deltaPnlCNS",
    "fundingCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "startDepositCNS",
    "endDepositCNS",
    "startLotLNS",
    "endLotLNS",
    "deltaPnlCNS",
    "fundingCNS",
]

[table.perpl__position_deleveraged]
description = "`PositionDeleveraged(uint256,uint256,bool,uint8,uint256,uint256,uint256,int256,int256,uint256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionDeleveraged(uint256,uint256,bool,uint8,uint256,uint256,uint256,int256,int256,uint256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__position_deleveraged.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
deleveragePricePNS = "The `deleveragePricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
deltaPnlCNS = "The `deltaPnlCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endDepositCNS = "The `endDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endLotLNS = "The `endLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
entryPricePNS = "The `entryPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
forceClose = "The `forceClose` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `forceClose = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `forceClose = 'true'` or `CAST(forceClose AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
fundingCNS = "The `fundingCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startDepositCNS = "The `startDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startLotLNS = "The `startLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_deleveraged.footguns]
big_ints = [
    "perpId",
    "accountId",
    "entryPricePNS",
    "markPricePNS",
    "deleveragePricePNS",
    "deltaPnlCNS",
    "fundingCNS",
    "startDepositCNS",
    "endDepositCNS",
    "startLotLNS",
    "endLotLNS",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "entryPricePNS",
    "markPricePNS",
    "deleveragePricePNS",
    "deltaPnlCNS",
    "fundingCNS",
    "startDepositCNS",
    "endDepositCNS",
    "startLotLNS",
    "endLotLNS",
    "amountCNS",
    "balanceCNS",
]
bools = ["forceClose"]

[table.perpl__position_deleveraged_v2]
description = "`PositionDeleveragedV2(uint256,uint256,bool,uint8,uint256,uint256,uint256,int256,int256,uint256,uint256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionDeleveragedV2(uint256,uint256,bool,uint8,uint256,uint256,uint256,int256,int256,uint256,uint256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__position_deleveraged_v2.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
deleveragePricePNS = "The `deleveragePricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
deltaPnlCNS = "The `deltaPnlCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endDepositCNS = "The `endDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endLotLNS = "The `endLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
entryPricePNS = "The `entryPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
forceClose = "The `forceClose` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `forceClose = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `forceClose = 'true'` or `CAST(forceClose AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
fundingCNS = "The `fundingCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
priceResiduePNSQ16 = "The `priceResiduePNSQ16` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startDepositCNS = "The `startDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startLotLNS = "The `startLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_deleveraged_v2.footguns]
big_ints = [
    "perpId",
    "accountId",
    "entryPricePNS",
    "markPricePNS",
    "deleveragePricePNS",
    "deltaPnlCNS",
    "fundingCNS",
    "startDepositCNS",
    "endDepositCNS",
    "startLotLNS",
    "endLotLNS",
    "amountCNS",
    "balanceCNS",
    "priceResiduePNSQ16",
]
overflows_dec = [
    "perpId",
    "accountId",
    "entryPricePNS",
    "markPricePNS",
    "deleveragePricePNS",
    "deltaPnlCNS",
    "fundingCNS",
    "startDepositCNS",
    "endDepositCNS",
    "startLotLNS",
    "endLotLNS",
    "amountCNS",
    "balanceCNS",
    "priceResiduePNSQ16",
]
bools = ["forceClose"]

[table.perpl__position_does_not_exist]
description = "`PositionDoesNotExist(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionDoesNotExist(uint256,uint256) event"

[table.perpl__position_does_not_exist.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_does_not_exist.footguns]
big_ints = [
    "perpId",
    "accountId",
]
overflows_dec = [
    "perpId",
    "accountId",
]

[table.perpl__position_increased]
description = "`PositionIncreased(uint256,uint256,uint8,uint256,uint256,uint256,int256,int256,uint256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionIncreased(uint256,uint256,uint8,uint256,uint256,uint256,int256,int256,uint256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__position_increased.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endDepositCNS = "The `endDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endLotLNS = "The `endLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
insFeeCNS = "The `insFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
leverageHdths = "The `leverageHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxNegPnlCollatBPS = "The `maxNegPnlCollatBPS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pnlCollateralizedCNS = "The `pnlCollateralizedCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
premiumPnlSettledCNS = "The `premiumPnlSettledCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
protFeeCNS = "The `protFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startDepositCNS = "The `startDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startLotLNS = "The `startLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_increased.footguns]
big_ints = [
    "perpId",
    "accountId",
    "leverageHdths",
    "startDepositCNS",
    "endDepositCNS",
    "pnlCollateralizedCNS",
    "premiumPnlSettledCNS",
    "maxNegPnlCollatBPS",
    "pricePNS",
    "startLotLNS",
    "endLotLNS",
    "insFeeCNS",
    "protFeeCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "leverageHdths",
    "startDepositCNS",
    "endDepositCNS",
    "pnlCollateralizedCNS",
    "premiumPnlSettledCNS",
    "maxNegPnlCollatBPS",
    "pricePNS",
    "startLotLNS",
    "endLotLNS",
    "insFeeCNS",
    "protFeeCNS",
]

[table.perpl__position_increased_v2]
description = "`PositionIncreasedV2(uint256,uint256,uint8,uint256,uint256,uint256,int256,int256,uint256,uint256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionIncreasedV2(uint256,uint256,uint8,uint256,uint256,uint256,int256,int256,uint256,uint256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__position_increased_v2.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endDepositCNS = "The `endDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endLotLNS = "The `endLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
insFeeCNS = "The `insFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
leverageHdths = "The `leverageHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
maxNegPnlCollatBPS = "The `maxNegPnlCollatBPS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pnlCollateralizedCNS = "The `pnlCollateralizedCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
premiumPnlSettledCNS = "The `premiumPnlSettledCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
priceResiduePNSQ16 = "The `priceResiduePNSQ16` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
protFeeCNS = "The `protFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startDepositCNS = "The `startDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startLotLNS = "The `startLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_increased_v2.footguns]
big_ints = [
    "perpId",
    "accountId",
    "leverageHdths",
    "startDepositCNS",
    "endDepositCNS",
    "pnlCollateralizedCNS",
    "premiumPnlSettledCNS",
    "maxNegPnlCollatBPS",
    "pricePNS",
    "startLotLNS",
    "endLotLNS",
    "insFeeCNS",
    "protFeeCNS",
    "priceResiduePNSQ16",
]
overflows_dec = [
    "perpId",
    "accountId",
    "leverageHdths",
    "startDepositCNS",
    "endDepositCNS",
    "pnlCollateralizedCNS",
    "premiumPnlSettledCNS",
    "maxNegPnlCollatBPS",
    "pricePNS",
    "startLotLNS",
    "endLotLNS",
    "insFeeCNS",
    "protFeeCNS",
    "priceResiduePNSQ16",
]

[table.perpl__position_inverted]
description = "`PositionInverted(uint256,uint256,uint8,uint256,uint256,uint256,int256,uint256,uint256,uint256,int256,int256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionInverted(uint256,uint256,uint8,uint256,uint256,uint256,int256,uint256,uint256,uint256,int256,int256,uint256,uint256) event"

[table.perpl__position_inverted.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
deltaPnlCNS = "The `deltaPnlCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endDepositCNS = "The `endDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endLotLNS = "The `endLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fundingCNS = "The `fundingCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
insFeeCNS = "The `insFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
leverageHdths = "The `leverageHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pnlCollateralizedCNS = "The `pnlCollateralizedCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
protFeeCNS = "The `protFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startDepositCNS = "The `startDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startLotLNS = "The `startLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_inverted.footguns]
big_ints = [
    "perpId",
    "accountId",
    "leverageHdths",
    "startDepositCNS",
    "endDepositCNS",
    "pnlCollateralizedCNS",
    "pricePNS",
    "startLotLNS",
    "endLotLNS",
    "deltaPnlCNS",
    "fundingCNS",
    "insFeeCNS",
    "protFeeCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "leverageHdths",
    "startDepositCNS",
    "endDepositCNS",
    "pnlCollateralizedCNS",
    "pricePNS",
    "startLotLNS",
    "endLotLNS",
    "deltaPnlCNS",
    "fundingCNS",
    "insFeeCNS",
    "protFeeCNS",
]

[table.perpl__position_liquidated]
description = "`PositionLiquidated(uint256,uint256,uint8,uint256,uint256,uint256,uint256,int256,int256,int256,uint256,int256,uint256,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionLiquidated(uint256,uint256,uint8,uint256,uint256,uint256,uint256,int256,int256,int256,uint256,int256,uint256,bool) event"

[table.perpl__position_liquidated.columns]
accAmountCNS = "The `accAmountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
accBalanceCNS = "The `accBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
deltaPnlCNS = "The `deltaPnlCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
fundingCNS = "The `fundingCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liqLotLNS = "The `liqLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
liqPricePNS = "The `liqPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
onOrderBook = "The `onOrderBook` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `onOrderBook = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `onOrderBook = 'true'` or `CAST(onOrderBook AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posAccountId = "The `posAccountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posAmountCNS = "The `posAmountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posDepositCNS = "The `posDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
posLotLNS = "The `posLotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_liquidated.footguns]
big_ints = [
    "perpId",
    "posAccountId",
    "markPricePNS",
    "liqPricePNS",
    "liqLotLNS",
    "posLotLNS",
    "deltaPnlCNS",
    "fundingCNS",
    "posAmountCNS",
    "posDepositCNS",
    "accAmountCNS",
    "accBalanceCNS",
]
overflows_dec = [
    "perpId",
    "posAccountId",
    "markPricePNS",
    "liqPricePNS",
    "liqLotLNS",
    "posLotLNS",
    "deltaPnlCNS",
    "fundingCNS",
    "posAmountCNS",
    "posDepositCNS",
    "accAmountCNS",
    "accBalanceCNS",
]
bools = ["onOrderBook"]

[table.perpl__position_liquidation_credit]
description = "`PositionLiquidationCredit(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionLiquidationCredit(uint256,uint256,uint256,uint256) event"

[table.perpl__position_liquidation_credit.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
endDepositCNS = "The `endDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
startDepositCNS = "The `startDepositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_liquidation_credit.footguns]
big_ints = [
    "perpId",
    "accountId",
    "startDepositCNS",
    "endDepositCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "startDepositCNS",
    "endDepositCNS",
]

[table.perpl__position_opened]
description = "`PositionOpened(uint256,uint256,uint8,uint256,uint256,int256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionOpened(uint256,uint256,uint8,uint256,uint256,int256,uint256,uint256,uint256,uint256) event"

[table.perpl__position_opened.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
depositCNS = "The `depositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
insFeeCNS = "The `insFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
leverageHdths = "The `leverageHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pnlCollateralizedCNS = "The `pnlCollateralizedCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
protFeeCNS = "The `protFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_opened.footguns]
big_ints = [
    "perpId",
    "accountId",
    "leverageHdths",
    "depositCNS",
    "pnlCollateralizedCNS",
    "pricePNS",
    "lotLNS",
    "insFeeCNS",
    "protFeeCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "leverageHdths",
    "depositCNS",
    "pnlCollateralizedCNS",
    "pricePNS",
    "lotLNS",
    "insFeeCNS",
    "protFeeCNS",
]

[table.perpl__position_opened_v2]
description = "`PositionOpenedV2(uint256,uint256,uint8,uint256,uint256,int256,uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionOpenedV2(uint256,uint256,uint8,uint256,uint256,int256,uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__position_opened_v2.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
depositCNS = "The `depositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
insFeeCNS = "The `insFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
leverageHdths = "The `leverageHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pnlCollateralizedCNS = "The `pnlCollateralizedCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
priceResiduePNSQ16 = "The `priceResiduePNSQ16` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
protFeeCNS = "The `protFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_opened_v2.footguns]
big_ints = [
    "perpId",
    "accountId",
    "leverageHdths",
    "depositCNS",
    "pnlCollateralizedCNS",
    "pricePNS",
    "lotLNS",
    "insFeeCNS",
    "protFeeCNS",
    "priceResiduePNSQ16",
]
overflows_dec = [
    "perpId",
    "accountId",
    "leverageHdths",
    "depositCNS",
    "pnlCollateralizedCNS",
    "pricePNS",
    "lotLNS",
    "insFeeCNS",
    "protFeeCNS",
    "priceResiduePNSQ16",
]

[table.perpl__position_unwound]
description = "`PositionUnwound(uint256,uint256,uint256,uint8,uint256,uint256,uint256,int256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionUnwound(uint256,uint256,uint256,uint8,uint256,uint256,uint256,int256,uint256,uint256) event"

[table.perpl__position_unwound.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
depositCNS = "The `depositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
paymentCNS = "The `paymentCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionFmvCNS = "The `positionFmvCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_unwound.footguns]
big_ints = [
    "perpId",
    "accountId",
    "markPricePNS",
    "pricePNS",
    "lotLNS",
    "depositCNS",
    "positionFmvCNS",
    "paymentCNS",
    "balanceCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "markPricePNS",
    "pricePNS",
    "lotLNS",
    "depositCNS",
    "positionFmvCNS",
    "paymentCNS",
    "balanceCNS",
]

[table.perpl__position_unwound_v2]
description = "`PositionUnwoundV2(uint256,uint256,uint256,uint8,uint256,uint256,uint256,int256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionUnwoundV2(uint256,uint256,uint256,uint8,uint256,uint256,uint256,int256,uint256,uint256,uint256) event"

[table.perpl__position_unwound_v2.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
depositCNS = "The `depositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
paymentCNS = "The `paymentCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionFmvCNS = "The `positionFmvCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
priceResiduePNSQ16 = "The `priceResiduePNSQ16` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_unwound_v2.footguns]
big_ints = [
    "perpId",
    "accountId",
    "markPricePNS",
    "pricePNS",
    "lotLNS",
    "depositCNS",
    "positionFmvCNS",
    "paymentCNS",
    "balanceCNS",
    "priceResiduePNSQ16",
]
overflows_dec = [
    "perpId",
    "accountId",
    "markPricePNS",
    "pricePNS",
    "lotLNS",
    "depositCNS",
    "positionFmvCNS",
    "paymentCNS",
    "balanceCNS",
    "priceResiduePNSQ16",
]

[table.perpl__position_unwound_without_payment]
description = "`PositionUnwoundWithoutPayment(uint256,uint256,uint256,uint8,uint256,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionUnwoundWithoutPayment(uint256,uint256,uint256,uint8,uint256,uint256,uint256,int256,uint256) event"

[table.perpl__position_unwound_without_payment.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountOwedCNS = "The `amountOwedCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
depositCNS = "The `depositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionFmvCNS = "The `positionFmvCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_unwound_without_payment.footguns]
big_ints = [
    "perpId",
    "accountId",
    "markPricePNS",
    "pricePNS",
    "lotLNS",
    "depositCNS",
    "positionFmvCNS",
    "amountOwedCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "markPricePNS",
    "pricePNS",
    "lotLNS",
    "depositCNS",
    "positionFmvCNS",
    "amountOwedCNS",
]

[table.perpl__position_unwound_without_payment_v2]
description = "`PositionUnwoundWithoutPaymentV2(uint256,uint256,uint256,uint8,uint256,uint256,uint256,int256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PositionUnwoundWithoutPaymentV2(uint256,uint256,uint256,uint8,uint256,uint256,uint256,int256,uint256,uint256) event"

[table.perpl__position_unwound_without_payment_v2.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountOwedCNS = "The `amountOwedCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
depositCNS = "The `depositCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
markPricePNS = "The `markPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionFmvCNS = "The `positionFmvCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionType = "The `positionType` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pricePNS = "The `pricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
priceResiduePNSQ16 = "The `priceResiduePNSQ16` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__position_unwound_without_payment_v2.footguns]
big_ints = [
    "perpId",
    "accountId",
    "markPricePNS",
    "pricePNS",
    "lotLNS",
    "depositCNS",
    "positionFmvCNS",
    "amountOwedCNS",
    "priceResiduePNSQ16",
]
overflows_dec = [
    "perpId",
    "accountId",
    "markPricePNS",
    "pricePNS",
    "lotLNS",
    "depositCNS",
    "positionFmvCNS",
    "amountOwedCNS",
    "priceResiduePNSQ16",
]

[table.perpl__post_order_under_minimum]
description = "`PostOrderUnderMinimum(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PostOrderUnderMinimum(uint256,uint256) event"

[table.perpl__post_order_under_minimum.columns]
minAmountCNS = "The `minAmountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderAmountCNS = "The `orderAmountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__post_order_under_minimum.footguns]
big_ints = [
    "orderAmountCNS",
    "minAmountCNS",
]
overflows_dec = [
    "orderAmountCNS",
    "minAmountCNS",
]

[table.perpl__price_administrator_updated]
description = "`PriceAdministratorUpdated(address,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PriceAdministratorUpdated(address,bool) event"

[table.perpl__price_administrator_updated.columns]
added = "The `added` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `added = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `added = 'true'` or `CAST(added AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
priceAdministrator = "The `priceAdministrator` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__price_administrator_updated.footguns]
bools = ["added"]

[table.perpl__price_max_age_updated]
description = "`PriceMaxAgeUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PriceMaxAgeUpdated(uint256,uint256) event"

[table.perpl__price_max_age_updated.columns]
maxAgeSec = "The `maxAgeSec` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__price_max_age_updated.footguns]
big_ints = [
    "perpId",
    "maxAgeSec",
]
overflows_dec = [
    "perpId",
    "maxAgeSec",
]

[table.perpl__price_out_of_range]
description = "`PriceOutOfRange(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PriceOutOfRange(uint256,uint256) event"

[table.perpl__price_out_of_range.columns]
maxPricePNS = "The `maxPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
minPricePNS = "The `minPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__price_out_of_range.footguns]
big_ints = [
    "minPricePNS",
    "maxPricePNS",
]
overflows_dec = [
    "minPricePNS",
    "maxPricePNS",
]

[table.perpl__price_set_during_trigger_exec]
description = "`PriceSetDuringTriggerExec(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PriceSetDuringTriggerExec(uint256) event"

[table.perpl__price_set_during_trigger_exec.columns]
triggerPricePNS = "The `triggerPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__price_set_during_trigger_exec.footguns]
big_ints = ["triggerPricePNS"]
overflows_dec = ["triggerPricePNS"]

[table.perpl__price_tol_updated]
description = "`PriceTolUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per PriceTolUpdated(uint256,uint256) event"

[table.perpl__price_tol_updated.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
tolPer100k = "The `tolPer100k` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__price_tol_updated.footguns]
big_ints = [
    "perpId",
    "tolPer100k",
]
overflows_dec = [
    "perpId",
    "tolPer100k",
]

[table.perpl__protocol_balance_deposit]
description = "`ProtocolBalanceDeposit(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ProtocolBalanceDeposit(uint256) event"

[table.perpl__protocol_balance_deposit.columns]
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__protocol_balance_deposit.footguns]
big_ints = ["amountCNS"]
overflows_dec = ["amountCNS"]

[table.perpl__protocol_balance_withdraw]
description = "`ProtocolBalanceWithdraw(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ProtocolBalanceWithdraw(uint256) event"

[table.perpl__protocol_balance_withdraw.columns]
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__protocol_balance_withdraw.footguns]
big_ints = ["amountCNS"]
overflows_dec = ["amountCNS"]

[table.perpl__recycle_balance_insufficient_severe]
description = "`RecycleBalanceInsufficientSevere(uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per RecycleBalanceInsufficientSevere(uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__recycle_balance_insufficient_severe.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recycleBalanceCNS = "The `recycleBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recycleFeeCNS = "The `recycleFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__recycle_balance_insufficient_severe.footguns]
big_ints = [
    "accountId",
    "perpId",
    "orderId",
    "recycleFeeCNS",
    "recycleBalanceCNS",
]
overflows_dec = [
    "accountId",
    "perpId",
    "orderId",
    "recycleFeeCNS",
    "recycleBalanceCNS",
]

[table.perpl__recycle_fee_to_account]
description = "`RecycleFeeToAccount(uint256,uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per RecycleFeeToAccount(uint256,uint256,uint256,uint256,uint256) event"

[table.perpl__recycle_fee_to_account.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recycleBalanceCNS = "The `recycleBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recycleFeeCNS = "The `recycleFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__recycle_fee_to_account.footguns]
big_ints = [
    "accountId",
    "perpId",
    "orderId",
    "recycleFeeCNS",
    "recycleBalanceCNS",
]
overflows_dec = [
    "accountId",
    "perpId",
    "orderId",
    "recycleFeeCNS",
    "recycleBalanceCNS",
]

[table.perpl__recycle_fee_to_protocol]
description = "`RecycleFeeToProtocol(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per RecycleFeeToProtocol(uint256,uint256,uint256,uint256) event"

[table.perpl__recycle_fee_to_protocol.columns]
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recycleBalanceCNS = "The `recycleBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
recycleFeeCNS = "The `recycleFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__recycle_fee_to_protocol.footguns]
big_ints = [
    "perpId",
    "orderId",
    "recycleFeeCNS",
    "recycleBalanceCNS",
]
overflows_dec = [
    "perpId",
    "orderId",
    "recycleFeeCNS",
    "recycleBalanceCNS",
]

[table.perpl__recycle_fee_updated]
description = "`RecycleFeeUpdated(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per RecycleFeeUpdated(uint256) event"

[table.perpl__recycle_fee_updated.columns]
recycleFeeCNS = "The `recycleFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__recycle_fee_updated.footguns]
big_ints = ["recycleFeeCNS"]
overflows_dec = ["recycleFeeCNS"]

[table.perpl__report_age_exceeds_last_update]
description = "`ReportAgeExceedsLastUpdate(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ReportAgeExceedsLastUpdate(uint256,uint256,uint256) event"

[table.perpl__report_age_exceeds_last_update.columns]
lastUpdateTimestamp = "The `lastUpdateTimestamp` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
reportValidFromTimestamp = "The `reportValidFromTimestamp` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__report_age_exceeds_last_update.footguns]
big_ints = [
    "perpId",
    "lastUpdateTimestamp",
    "reportValidFromTimestamp",
]
overflows_dec = [
    "perpId",
    "lastUpdateTimestamp",
    "reportValidFromTimestamp",
]

[table.perpl__report_expires_too_soon]
description = "`ReportExpiresTooSoon(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ReportExpiresTooSoon(uint256,uint256,uint256) event"

[table.perpl__report_expires_too_soon.columns]
expiresAt = "The `expiresAt` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
minRequired = "The `minRequired` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__report_expires_too_soon.footguns]
big_ints = [
    "perpId",
    "expiresAt",
    "minRequired",
]
overflows_dec = [
    "perpId",
    "expiresAt",
    "minRequired",
]

[table.perpl__report_from_future]
description = "`ReportFromFuture(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ReportFromFuture(uint256,uint256,uint256) event"

[table.perpl__report_from_future.columns]
blockTimestamp = "The `blockTimestamp` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
reportTimestamp = "The `reportTimestamp` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__report_from_future.footguns]
big_ints = [
    "perpId",
    "reportTimestamp",
    "blockTimestamp",
]
overflows_dec = [
    "perpId",
    "reportTimestamp",
    "blockTimestamp",
]

[table.perpl__report_price_is_negative]
description = "`ReportPriceIsNegative(uint256,int256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ReportPriceIsNegative(uint256,int256) event"

[table.perpl__report_price_is_negative.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
reportPrice = "The `reportPrice` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__report_price_is_negative.footguns]
big_ints = [
    "perpId",
    "reportPrice",
]
overflows_dec = [
    "perpId",
    "reportPrice",
]

[table.perpl__residue_balance_insufficient]
description = "`ResidueBalanceInsufficient(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ResidueBalanceInsufficient(uint256,uint256,uint256) event"

[table.perpl__residue_balance_insufficient.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionBalanceCNS = "The `positionBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
requestedAmountCNS = "The `requestedAmountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__residue_balance_insufficient.footguns]
big_ints = [
    "perpId",
    "requestedAmountCNS",
    "positionBalanceCNS",
]
overflows_dec = [
    "perpId",
    "requestedAmountCNS",
    "positionBalanceCNS",
]

[table.perpl__residue_transferred]
description = "`ResidueTransferred(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ResidueTransferred(uint256,uint256,uint256) event"

[table.perpl__residue_transferred.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionBalanceCNS = "The `positionBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
residueAmountCNS = "The `residueAmountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__residue_transferred.footguns]
big_ints = [
    "perpId",
    "residueAmountCNS",
    "positionBalanceCNS",
]
overflows_dec = [
    "perpId",
    "residueAmountCNS",
    "positionBalanceCNS",
]

[table.perpl__taker_fee_updated]
description = "`TakerFeeUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TakerFeeUpdated(uint256,uint256) event"

[table.perpl__taker_fee_updated.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
takerFeePer100K = "The `takerFeePer100K` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__taker_fee_updated.footguns]
big_ints = [
    "perpId",
    "takerFeePer100K",
]
overflows_dec = [
    "perpId",
    "takerFeePer100K",
]

[table.perpl__taker_order_filled]
description = "`TakerOrderFilled(uint256,uint256,uint256,uint256,uint256,int256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TakerOrderFilled(uint256,uint256,uint256,uint256,uint256,int256,uint256) event"

[table.perpl__taker_order_filled.columns]
amountCNS = "The `amountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
collatPricePNS = "The `collatPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
entryPricePNS = "The `entryPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
feeCNS = "The `feeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pnlPricePNS = "The `pnlPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__taker_order_filled.footguns]
big_ints = [
    "entryPricePNS",
    "collatPricePNS",
    "pnlPricePNS",
    "lotLNS",
    "feeCNS",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "entryPricePNS",
    "collatPricePNS",
    "pnlPricePNS",
    "lotLNS",
    "feeCNS",
    "amountCNS",
    "balanceCNS",
]

[table.perpl__taker_order_filled_v2]
description = "`TakerOrderFilledV2(uint256,uint256,uint256,uint256,uint256,int256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TakerOrderFilledV2(uint256,uint256,uint256,uint256,uint256,int256,uint256,uint256,uint256) event"

[table.perpl__taker_order_filled_v2.columns]
amountCNS = "The `amountCNS` int256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
builderFeeCNS = "The `builderFeeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
builderId = "The `builderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
collatPricePNS = "The `collatPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
entryPricePNS = "The `entryPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
feeCNS = "The `feeCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
lotLNS = "The `lotLNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
pnlPricePNS = "The `pnlPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__taker_order_filled_v2.footguns]
big_ints = [
    "entryPricePNS",
    "collatPricePNS",
    "pnlPricePNS",
    "lotLNS",
    "feeCNS",
    "amountCNS",
    "balanceCNS",
    "builderId",
    "builderFeeCNS",
]
overflows_dec = [
    "entryPricePNS",
    "collatPricePNS",
    "pnlPricePNS",
    "lotLNS",
    "feeCNS",
    "amountCNS",
    "balanceCNS",
    "builderId",
    "builderFeeCNS",
]

[table.perpl__tolerance_administrator_updated]
description = "`ToleranceAdministratorUpdated(address,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ToleranceAdministratorUpdated(address,bool) event"

[table.perpl__tolerance_administrator_updated.columns]
added = "The `added` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `added = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `added = 'true'` or `CAST(added AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"
toleranceAdministrator = "The `toleranceAdministrator` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__tolerance_administrator_updated.footguns]
bools = ["added"]

[table.perpl__transfer_account_to_protocol]
description = "`TransferAccountToProtocol(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TransferAccountToProtocol(uint256,uint256,uint256) event"

[table.perpl__transfer_account_to_protocol.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__transfer_account_to_protocol.footguns]
big_ints = [
    "accountId",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "accountId",
    "amountCNS",
    "balanceCNS",
]

[table.perpl__transfer_perp_ins_to_protocol]
description = "`TransferPerpInsToProtocol(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TransferPerpInsToProtocol(uint256,uint256) event"

[table.perpl__transfer_perp_ins_to_protocol.columns]
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__transfer_perp_ins_to_protocol.footguns]
big_ints = [
    "perpId",
    "amountCNS",
]
overflows_dec = [
    "perpId",
    "amountCNS",
]

[table.perpl__transfer_perp_pos_to_protocol]
description = "`TransferPerpPosToProtocol(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TransferPerpPosToProtocol(uint256,uint256) event"

[table.perpl__transfer_perp_pos_to_protocol.columns]
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__transfer_perp_pos_to_protocol.footguns]
big_ints = [
    "perpId",
    "amountCNS",
]
overflows_dec = [
    "perpId",
    "amountCNS",
]

[table.perpl__transfer_protocol_to_account]
description = "`TransferProtocolToAccount(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TransferProtocolToAccount(uint256,uint256,uint256) event"

[table.perpl__transfer_protocol_to_account.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
balanceCNS = "The `balanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__transfer_protocol_to_account.footguns]
big_ints = [
    "accountId",
    "amountCNS",
    "balanceCNS",
]
overflows_dec = [
    "accountId",
    "amountCNS",
    "balanceCNS",
]

[table.perpl__transfer_protocol_to_perp]
description = "`TransferProtocolToPerp(uint256,uint256,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TransferProtocolToPerp(uint256,uint256,bool) event"

[table.perpl__transfer_protocol_to_perp.columns]
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
toInsuranceFund = "The `toInsuranceFund` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `toInsuranceFund = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `toInsuranceFund = 'true'` or `CAST(toInsuranceFund AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__transfer_protocol_to_perp.footguns]
big_ints = [
    "perpId",
    "amountCNS",
]
overflows_dec = [
    "perpId",
    "amountCNS",
]
bools = ["toInsuranceFund"]

[table.perpl__transfer_protocol_to_recycle_bal]
description = "`TransferProtocolToRecycleBal(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TransferProtocolToRecycleBal(uint256) event"

[table.perpl__transfer_protocol_to_recycle_bal.columns]
amountCNS = "The `amountCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__transfer_protocol_to_recycle_bal.footguns]
big_ints = ["amountCNS"]
overflows_dec = ["amountCNS"]

[table.perpl__trigger_desc_id_too_low]
description = "`TriggerDescIdTooLow(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TriggerDescIdTooLow(uint256) event"

[table.perpl__trigger_desc_id_too_low.columns]
lastTriggerDescId = "The `lastTriggerDescId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__trigger_desc_id_too_low.footguns]
big_ints = ["lastTriggerDescId"]
overflows_dec = ["lastTriggerDescId"]

[table.perpl__trigger_order_execution]
description = "`TriggerOrderExecution()` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TriggerOrderExecution() event"

[table.perpl__trigger_order_request]
description = "`TriggerOrderRequest(uint256,uint8,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per TriggerOrderRequest(uint256,uint8,uint256,uint256) event"

[table.perpl__trigger_order_request.columns]
triggerPositionId = "The `triggerPositionId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
triggerPriceCondition = "The `triggerPriceCondition` uint8 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
triggerPricePNS = "The `triggerPricePNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
triggerRequestId = "The `triggerRequestId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__trigger_order_request.footguns]
big_ints = [
    "triggerPricePNS",
    "triggerRequestId",
    "triggerPositionId",
]
overflows_dec = [
    "triggerPricePNS",
    "triggerRequestId",
    "triggerPositionId",
]

[table.perpl__unable_to_cancel_order]
description = "`UnableToCancelOrder(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnableToCancelOrder(uint256,uint256) event"

[table.perpl__unable_to_cancel_order.columns]
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unable_to_cancel_order.footguns]
big_ints = [
    "perpId",
    "orderId",
]
overflows_dec = [
    "perpId",
    "orderId",
]

[table.perpl__unity_descent_thresh_updated]
description = "`UnityDescentThreshUpdated(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnityDescentThreshUpdated(uint256,uint256) event"

[table.perpl__unity_descent_thresh_updated.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
threshHdths = "The `threshHdths` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unity_descent_thresh_updated.footguns]
big_ints = [
    "perpId",
    "threshHdths",
]
overflows_dec = [
    "perpId",
    "threshHdths",
]

[table.perpl__unspecified_collateral]
description = "`UnspecifiedCollateral()` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnspecifiedCollateral() event"

[table.perpl__unwind_completed]
description = "`UnwindCompleted(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnwindCompleted(uint256,uint256,uint256,uint256) event"

[table.perpl__unwind_completed.columns]
insuranceBalanceCNS = "The `insuranceBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpPositionBalanceCNS = "The `perpPositionBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionsUnwound = "The `positionsUnwound` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unwind_completed.footguns]
big_ints = [
    "perpId",
    "positionsUnwound",
    "perpPositionBalanceCNS",
    "insuranceBalanceCNS",
]
overflows_dec = [
    "perpId",
    "positionsUnwound",
    "perpPositionBalanceCNS",
    "insuranceBalanceCNS",
]

[table.perpl__unwind_contract_trigger]
description = "`UnwindContractTrigger(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnwindContractTrigger(uint256) event"

[table.perpl__unwind_contract_trigger.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unwind_contract_trigger.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__unwind_initialization_cleared]
description = "`UnwindInitializationCleared(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnwindInitializationCleared(uint256) event"

[table.perpl__unwind_initialization_cleared.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unwind_initialization_cleared.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__unwind_initialized]
description = "`UnwindInitialized(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnwindInitialized(uint256,uint256) event"

[table.perpl__unwind_initialized.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
sumPositiveFmvCNS = "The `sumPositiveFmvCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unwind_initialized.footguns]
big_ints = [
    "perpId",
    "sumPositiveFmvCNS",
]
overflows_dec = [
    "perpId",
    "sumPositiveFmvCNS",
]

[table.perpl__unwind_insufficient_balance]
description = "`UnwindInsufficientBalance(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnwindInsufficientBalance(uint256,uint256,uint256,uint256) event"

[table.perpl__unwind_insufficient_balance.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
paymentCNS = "The `paymentCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpPositionBalanceCNS = "The `perpPositionBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unwind_insufficient_balance.footguns]
big_ints = [
    "perpId",
    "accountId",
    "perpPositionBalanceCNS",
    "paymentCNS",
]
overflows_dec = [
    "perpId",
    "accountId",
    "perpPositionBalanceCNS",
    "paymentCNS",
]

[table.perpl__unwind_iteration_completed]
description = "`UnwindIterationCompleted(uint256,uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnwindIterationCompleted(uint256,uint256,uint256,uint256) event"

[table.perpl__unwind_iteration_completed.columns]
insuranceBalanceCNS = "The `insuranceBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpPositionBalanceCNS = "The `perpPositionBalanceCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
positionsUnwound = "The `positionsUnwound` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unwind_iteration_completed.footguns]
big_ints = [
    "perpId",
    "positionsUnwound",
    "perpPositionBalanceCNS",
    "insuranceBalanceCNS",
]
overflows_dec = [
    "perpId",
    "positionsUnwound",
    "perpPositionBalanceCNS",
    "insuranceBalanceCNS",
]

[table.perpl__unwind_preparation_cleared]
description = "`UnwindPreparationCleared(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnwindPreparationCleared(uint256) event"

[table.perpl__unwind_preparation_cleared.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unwind_preparation_cleared.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__unwind_prepared]
description = "`UnwindPrepared(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnwindPrepared(uint256) event"

[table.perpl__unwind_prepared.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unwind_prepared.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__unwind_process_in_progress]
description = "`UnwindProcessInProgress(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UnwindProcessInProgress(uint256) event"

[table.perpl__unwind_process_in_progress.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__unwind_process_in_progress.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__update_oracle_failed]
description = "`UpdateOracleFailed(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per UpdateOracleFailed(uint256) event"

[table.perpl__update_oracle_failed.columns]
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__update_oracle_failed.footguns]
big_ints = ["perpId"]
overflows_dec = ["perpId"]

[table.perpl__upgraded]
description = "`Upgraded(address)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per Upgraded(address) event"

[table.perpl__upgraded.columns]
implementation = "The `implementation` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__value_exceeds_maximum]
description = "`ValueExceedsMaximum(uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per ValueExceedsMaximum(uint256,uint256) event"

[table.perpl__value_exceeds_maximum.columns]
maximum = "The `maximum` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
value = "The `value` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__value_exceeds_maximum.footguns]
big_ints = [
    "value",
    "maximum",
]
overflows_dec = [
    "value",
    "maximum",
]

[table.perpl__w_r_l_s_min_withdraw_limit_updated]
description = "`WRLSMinWithdrawLimitUpdated(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per WRLSMinWithdrawLimitUpdated(uint256) event"

[table.perpl__w_r_l_s_min_withdraw_limit_updated.columns]
limitCNS = "The `limitCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__w_r_l_s_min_withdraw_limit_updated.footguns]
big_ints = ["limitCNS"]
overflows_dec = ["limitCNS"]

[table.perpl__w_r_l_s_thousandths_tvl_updated]
description = "`WRLSThousandthsTvlUpdated(uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per WRLSThousandthsTvlUpdated(uint256) event"

[table.perpl__w_r_l_s_thousandths_tvl_updated.columns]
thousandthsTvl = "The `thousandthsTvl` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__w_r_l_s_thousandths_tvl_updated.footguns]
big_ints = ["thousandthsTvl"]
overflows_dec = ["thousandthsTvl"]

[table.perpl__whitelist_address]
description = "`WhitelistAddress(address,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per WhitelistAddress(address,bool) event"

[table.perpl__whitelist_address.columns]
addr = "The `addr` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
whitelisted = "The `whitelisted` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `whitelisted = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `whitelisted = 'true'` or `CAST(whitelisted AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__whitelist_address.footguns]
bools = ["whitelisted"]

[table.perpl__whitelisting_enabled_changed]
description = "`WhitelistingEnabledChanged(bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per WhitelistingEnabledChanged(bool) event"

[table.perpl__whitelisting_enabled_changed.columns]
enabled = "The `enabled` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `enabled = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `enabled = 'true'` or `CAST(enabled AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__whitelisting_enabled_changed.footguns]
bools = ["enabled"]

[table.perpl__withdraw_rate_limit_bypass_set]
description = "`WithdrawRateLimitBypassSet(address,bool)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per WithdrawRateLimitBypassSet(address,bool) event"

[table.perpl__withdraw_rate_limit_bypass_set.columns]
addr = "The `addr` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
enabled = "The `enabled` bool parameter - stored as exact text `'true'`/`'false'`, not a SQL boolean. `enabled = true` and `AND`/`NOT` implicitly cast and work; `COALESCE`, `CASE`, `bool_and`/`bool_or` and `UNION` do not. Write `enabled = 'true'` or `CAST(enabled AS BOOLEAN)`. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__withdraw_rate_limit_bypass_set.footguns]
bools = ["enabled"]

[table.perpl__withdraw_rate_limit_force_reset]
description = "`WithdrawRateLimitForceReset(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per WithdrawRateLimitForceReset(uint256,uint256,uint256) event"

[table.perpl__withdraw_rate_limit_force_reset.columns]
newExpiryBlock = "The `newExpiryBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
newLimitCNS = "The `newLimitCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perBlockCNS = "The `perBlockCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__withdraw_rate_limit_force_reset.footguns]
big_ints = [
    "newExpiryBlock",
    "newLimitCNS",
    "perBlockCNS",
]
overflows_dec = [
    "newExpiryBlock",
    "newLimitCNS",
    "perBlockCNS",
]

[table.perpl__withdraw_rate_limit_reset]
description = "`WithdrawRateLimitReset(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per WithdrawRateLimitReset(uint256,uint256,uint256) event"

[table.perpl__withdraw_rate_limit_reset.columns]
newExpiryBlock = "The `newExpiryBlock` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
newLimitCNS = "The `newLimitCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perBlockCNS = "The `perBlockCNS` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__withdraw_rate_limit_reset.footguns]
big_ints = [
    "newExpiryBlock",
    "newLimitCNS",
    "perBlockCNS",
]
overflows_dec = [
    "newExpiryBlock",
    "newLimitCNS",
    "perBlockCNS",
]

[table.perpl__wrong_account_for_order]
description = "`WrongAccountForOrder(uint256,uint256,uint256)` events emitted by the `perpl` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per WrongAccountForOrder(uint256,uint256,uint256) event"

[table.perpl__wrong_account_for_order.columns]
accountId = "The `accountId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
orderId = "The `orderId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
perpId = "The `perpId` uint256 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[table.perpl__wrong_account_for_order.footguns]
big_ints = [
    "perpId",
    "orderId",
    "accountId",
]
overflows_dec = [
    "perpId",
    "orderId",
    "accountId",
]

[view]

# Authored views (views/*.sql) are described here so the MCP/`/schema` can render them:
# [view.your_view_name]
# description = "What this derivation computes."
